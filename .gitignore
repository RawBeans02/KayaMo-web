node_modules/
.next/
out/
dist/
.turbo/
coverage/
*.tsbuildinfo
*.log
.env
.env.local
.env.*.local
.DS_Store
playwright-report/
test-results/
output/imagegen/

# Design zip drops — never commit the HTML prototypes
KayaMo mobile app design *
kayamo-android-assets.zip
apps/pwa/.capacitor-parked/

# Local HTML drop folder. Canonical copies live in docs/design/desktop-handoff/.
design_handoff_kayamo_desktop/

# Capacitor native projects — generated, but commit them once you ship
apps/mobile/ios/App/Pods/
apps/mobile/android/.gradle/
apps/mobile/android/app/build/
apps/mobile/android/local.properties
*.keystore
*.jks
apps/mobile/android/keystore.properties
apps/mobile/www/**
!apps/mobile/www/.gitkeep

# Supabase local
supabase/.branches/
supabase/.temp/

# Generated by `next dev`/`next build`; Next's docs say not to commit it.
next-env.d.ts

# Agent/tooling output.
graphify-out/

# Raw drops at the repo root. The knowledge-base build scripts read data/gym/csv
# and data/todo/csv, never these. NOTE: gym_knowledge_base_v1/ is NOT a copy of
# data/gym — it is newer (extra app_workflow/rest_timer keys in BUILD_SUMMARY,
# different exercise flags) and has never been promoted. Ignoring the path keeps
# the files on disk; promoting them is a deliberate content decision.
conversational_calorie_tracker_v1/
gym_knowledge_base_v1/
