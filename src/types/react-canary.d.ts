/// <reference types="react/canary" />
/*
 * The App Router ships React's canary channel, which includes the
 * <ViewTransition> component the route transitions use. @types/react keeps
 * its declaration behind this reference so stable-channel users do not see
 * it; this file opts the web app in. Nothing else from the canary channel is
 * used.
 */
