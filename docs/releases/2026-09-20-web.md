# Web release, 2026-09-20 — v1 to production

**Merge:** PR #1 `codex/botanical-liquid-glass` → `main`, merge commit `8785853`
(66 commits, Phases 0–6 of the 2026-09-18 review).
**Vercel:** production deployment of `8785853`, built 2026-09-19 16:17 UTC
(GitHub deployment 6542996763), deployment URL
`kaya-mo-web-ojxe3swb3-rovinceeduvane113-2617s-projects.vercel.app`.

## State at the time of writing

Deployed, verified, **not yet reachable on kayamo.fit.** `kayamo.fit` and
`www.kayamo.fit` still resolve to the registrar's parking address
(`198.54.117.242`), so nothing answers on 443 there. The production deployment
URL is behind Vercel's Standard Deployment Protection, which is expected: that
setting protects generated URLs and leaves the custom domain public once it is
attached. Two owner steps make the site public:

1. Vercel → project → Settings → Domains: add `www.kayamo.fit` (primary) and
   `kayamo.fit` (redirect to www).
2. At the registrar: `www` CNAME → `cname.vercel-dns.com`; apex A → `76.76.21.21`
   (or the values Vercel shows). Wait for DNS, then re-run the checks below on
   the real host.

## Verified on the production deployment (through the automation bypass)

- Routes: `/`, `/login`, `/privacy`, `/terms`, `/accessibility`, `/robots.txt`,
  `/sitemap.xml` 200; `/today` signed out 307 → `/login?next=/today`; unknown
  path a real 404; `/api/mus/permissions` 401 signed out.
- Headers: HSTS two years with subdomains, `frame-ancestors 'none'`,
  `X-Frame-Options: DENY`, nosniff, `strict-origin-when-cross-origin`,
  Permissions-Policy. `x-robots-tag: noindex` is Vercel's own header on
  generated deployment URLs; the app's robots allows `/` and disallows `/api/`
  and `/auth/`, and the sitemap lists `/`, `/privacy`, `/terms`,
  `/accessibility` on `https://www.kayamo.fit`. Canonical on `/` is
  `https://www.kayamo.fit`.
- Functions ran in `iad1` (`x-vercel-id`), database in `ap-south-1`; the
  region item in `docs/RELEASE.md` stands.

## Results

- Preview smoke and Lighthouse (same build, PR #1 preview, 2026-09-19): in
  `docs/RELEASE.md` under *Add preview smoke*.
- Production, Chromium suite in hosted mode (main's specs, through the bypass):
  31 passed, 28 skipped (the skips are the local-only specs: skip-login,
  mocked-auth entry pages, dev gallery, bundle budget), 0 failed.

## Still open after this release

- Domain and DNS (above); then apex → www, HTTPS and canonical on the real host.
- Legal review before the draft banner comes off (`LEGAL_APPROVED_ON`).
- Vercel function region → `bom1`; monitoring sink; Search Console; backups
  confirmation; auth rate limits (see the owner list in `docs/RELEASE.md`).
- Sign-in by password lands with Phase 8 (`phase-8/password-sign-in`); until it
  is deployed, production sign-in is the emailed link.
- Rollback: the previous production deployment (the pre-v1 build) stays listed
  in Vercel; promote it from the Deployments tab if needed.
