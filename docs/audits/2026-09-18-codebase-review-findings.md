# Codebase review findings appendix — 2026-09-18

Companion to `2026-09-18-codebase-review.md`. One line per finding, grouped by area. Severity shown is the reviewer's; for verified P0/P1 items the verifier's severity follows an arrow. `confirmed` = independently re-checked in code by a second agent; `reviewer-judgment` = structural P1 not independently re-checked; P2/P3 were not re-checked.

Totals: 290 findings, 54 P0/P1 → 37 after dedup → 27 verified (0 refuted), 10 reviewer-judgment, 236 P2/P3.


## root-app

- **P1** `security` (S, high) [confirmed] — Open redirect in /auth/callback: `next` check accepts protocol-relative and backslash paths — `src/app/auth/callback/route.ts:11`
  - Fix: Harden authCallbackNextPath: require `/^\/(?![\/\\])/.test(next)` and reject any value containing `\`, `//`, `:` or whitespace; better, resolve `new URL(next, origin)` and assert `.origin === origin` before redirecting. Add unit cases for `//evil`, `/\\evil`, `/%2F%2Fevil`, `javascript:`. Exploit needs a valid one-time code so practical impact is bounded, but it is a two-line fix and a standard pre-ship gate.
  - Verifier: Confirmed at /Users/rovs/Documents/KayaMo/kayamo-web/packages/features/src/auth/paths.ts:16-19 (only `startsWith('/')`) and route.ts:12 (`new URL(next, url.origin)` then `NextResponse.redirect`); node reproduces `//evil.example/x` -> https://evil.example/x and `/\\evil.example` -> https://evil.example/. No other guard exists: src/lib/auth-landing.ts:32 and src/proxy.ts:62 propagate `next` unvalida
- **P1→P2** `security` (S, high) [confirmed] — /auth/set-session logs the browser into any tokens in the URL fragment (login-CSRF) and is unreferenced on web — `src/app/auth/set-session/page.tsx:6`
  - Fix: Delete src/app/auth/set-session/ and src/app/auth/complete/ from the web app (web uses the PKCE /auth/callback route handler; complete-session.tsx's own comment says it is for shells without that handler). If a hosted e2e helper ever needs set-session, gate the page behind `isLocalDevLoginEnabled()` and `notFound()` otherwise. Keep the packages/features components for mobile parity.
  - Verifier: Confirmed at the cited location: src/app/auth/set-session/page.tsx renders SetAuthSession, which unconditionally reads access_token/refresh_token from the URL hash and calls supabase.auth.setSession, with no gate (isLocalDevLoginEnabled is only used by /login and /api/auth/local-dev) and no producer on web (only ../kayamo-mobile/e2e/helpers/auth.ts references it); /auth/complete is likewise dead s
- **P1** `release` (M, high) [confirmed] — Landing promises data export that Settings says does not exist; no privacy/terms links anywhere in the app — `src/app/landing/landing.tsx:85`
  - Fix: Remove or reword the export sentence now (S). Before ship, add a /legal (or /privacy, /terms, /support) route group rendering the docs/legal drafts once finalized, link them from landing footer, login footer and the Settings 'Privacy & accessibility' row, and either implement account deletion (a server route calling `auth.admin.deleteUser` plus RLS-scoped row deletes, behind re-auth) or state the email-based deletion path from docs/legal/support-and-issue-reporting.md:55 in the Settings row instead of an inert 'Not available' control.
  - Verifier: Verified: src/app/landing/landing.tsx:85-86 says "Export it whenever you want." while src/app/(shell)/settings/page.tsx:159-172 renders inert "Export my data" and "Delete account" rows marked "Not available on the web yet."; no href to a privacy notice, terms or support address exists anywhere under src/ (landing footer :170-174, login footer login-view.tsx:72-74, settings privacy body :129-151 sa
- **P2** `bug` (S, high) — Post-login `?next=` is set by the proxy but never consumed; users always land on /today — `src/proxy.ts:62`
- **P2** `bug` (S, high) — Landing and shell layout call createServerSupabase() unguarded, so an unconfigured deployment 500s instead of reaching the setup banner — `src/app/page.tsx:32`
- **P2** `refactor` (S, high) — Identity resolved 2-3 times per request across layout, page and settings; guest check duplicated — `src/lib/shell-user.ts:13`
- **P2** `boundary` (M, high) — Product UI logic lives in root src/shell (log-sheet create surface, companion-settings feature screen) — `src/shell/log-sheet.tsx:131`
- **P2** `dead-code` (S, high) — Six dead shell files/exports from two earlier shell generations, plus a hidden wash element — `src/shell/botanical-shell.module.css:1`
- **P2** `design` (M, high) — Login page is still on the legacy --color-* token layer while landing/shell/settings use glass tokens — `src/app/login/login.module.css:5`
- **P2** `design` (S, high) — Language is half-retired: boot script and login heading keep Taglish, the only toggle is dead, companion settings offers it — `src/app/layout.tsx:46`
- **P2** `design` (S, high) — Demo guests are shown the companion-settings editor, which always fails with a misleading 'unavailable' message — `src/app/(shell)/settings/page.tsx:115`
- **P2** `security` (S, high) — No security headers (frame-ancestors/CSP, Referrer-Policy, Permissions-Policy) configured — `next.config.ts:6`
- **P2** `test-gap` (S, high) — No unit test for proxy gating, the legacy redirect map, or protocol-relative `next`; e2e never round-trips a deep link through login — `src/proxy.ts:23`
- **P2** `docs` (S, high) — .cursor/rules and tsconfig describe a layout, market and assistant name this repo no longer has — `.cursor/rules/000-project.mdc:7`
- **P2** `release` (S, medium) — Dev uses Turbopack, build uses `--webpack`; the backdrop-filter CSS-module workaround was validated against an unknown bundler — `package.json:14`
- **P2** `bug` (S, high) — Legacy /app/* redirect map points at wrong destinations now that /settings, /goals, /life, /grove exist — `src/app/app/[[...slug]]/page.tsx:3`
- **P2** `bug` (S, high) — /settings is not in the proxy matcher or PROTECTED set, so it bypasses middleware gating and cookie refresh — `src/proxy.ts:5`
- **P3** `bug` (S, high) — SignOutButton references a class that does not exist in the module it imports — `src/shell/sign-out-button.tsx:15`
- **P3** `design` (S, high) — Manifest colours and missing theme-color use the retired botanical palette — `src/app/manifest.ts:10`
- **P3** `design` (S, high) — Page titles are inconsistent: no title template, so /settings shows just 'Profile' — `src/app/layout.tsx:18`
- **P3** `a11y` (S, medium) — Settings promises 44px targets and full keyboard reach, but the shell's phone 'Ask Lis' button is 40px and the log sheet has no focus trap or focus return — `src/shell/desktop-shell.tsx:200`
- **P3** `docs` (S, high) — Guest-mode comments claim 'no server read path at all', but the demo does read the public foods table — `src/lib/guest.ts:4`
- **P3** `perf` (S, medium) — Public landing is fully dynamic and calls Supabase auth on every anonymous visit — `src/app/page.tsx:32`
- **P3** `duplication` (S, high) — Production origin hardcoded in two constants and an env var — `src/lib/site-metadata.ts:3`
- **P3** `security` (S, medium) — next.config.ts loads .env.local from up to three directories above the repo — `next.config.ts:4`

## api-routes

- **P1** `release` (S, high) [confirmed] — .env.example documents 3 of ~20-25 environment variables and claims the service-role key is not needed, yet every AI route 503s without SUPABASE_SERVICE_ROLE_KEY and OPENAI_API_KEY, MODEL_*/MUS_*_MODEL, USDA_FDC_API_KEY, DATABASE_URL and others are read undocumented — `.env.example:4` (dedups tests-ci-tooling-release-6)
  - Fix: Rewrite .env.example (and a docs/ops/environment.md) with the full inventory, grouped. CLIENT-INLINED (NEXT_PUBLIC_*): SUPABASE_URL [required], SUPABASE_ANON_KEY [required], SITE_URL [optional; ports.ts:18 falls back to VERCEL_* then pinned www.kayamo.fit], API_ORIGIN [optional, Capacitor only; api-origin.ts:52], VAPID_PUBLIC_KEY [optional; daily-loop.tsx:267 — note .env.local defines VAPID_PUBLIC_KEY without the NEXT_PUBLIC_ prefix, so it is never read], BUILD_ID [optional; diagnostics-panel.tsx:80]. SERVER-ONLY REQUIRED FOR AI: SUPABASE_SERVICE_ROLE_KEY (allowance RPC + dev login), OPENAI_AP
  - Verifier: Verified: kayamo-web/.env.example documents only NEXT_PUBLIC_SUPABASE_URL/ANON_KEY/SITE_URL and its line-4 comment "Anon key only — the service-role boundary belongs to apps/admin" is factually wrong, since src/lib/server-ai-allowance.ts calls createServiceSupabase() (packages/db/src/env.ts throws on missing SUPABASE_SERVICE_ROLE_KEY) and returns 503 for every AI route without it; the header's "re
- **P2** `bug` (M, high) — /api/foods/parse can never reach the model: parseFoodMessage is called without a budget gate, completeObject refuses, and the heuristic is silently returned (open since the Sep 8 audit) — `src/app/api/foods/parse/route.ts:39`
- **P2** `bug` (S, high) — A Lis turn reserves an allowance slot, then can 500 on an unguarded profiles/agent_runs query, burning the slot with no fallback reply — `packages/features/src/mus/server-context.ts:47`
- **P2** `perf` (M, high) — Worldwide search rate limit is a per-process in-memory counter: not global across Vercel instances, not per user, and one user can exhaust it for everyone on that instance — `packages/features/src/food/worldwide-search-server.ts:3`
- **P2** `refactor` (M, high) — Route plumbing is copy-pasted: the auth preamble in 9 routes, nonnegativeEnvNumber 3x, the agent_runs telemetry insert 5x, describeCause/authenticatedClient 2x, no-store headers on only 3 of 12 routes — `src/app/api/gym/consult/route.ts:21`
- **P2** `test-gap` (M, high) — server-plan.ts (4 AI handlers), gym consult, permissions, profile, demo, local-dev and /auth/callback have no tests; RLS suite lacks lis_companion_profile and reserve_web_ai_request — `packages/features/src/mus/server-plan.ts:135`
- **P2** `security` (S, medium) — Bearer-token origin allow-list still includes https://kayamo.ph from the Philippines-first era — `packages/features/src/api/api-origin.ts:29`
- **P3** `security` (S, high) — authCallbackNextPath accepts protocol-relative and backslash paths, so /auth/callback?next=//evil.example redirects off-site after a successful sign-in — `packages/features/src/auth/paths.ts:18`
- **P3** `security` (S, high) — observe-image trusts the client-declared mediaType, has a dead try/catch around Buffer.from, and the PHOTO_ANALYSES_PER_DAY knob in .env.local is never read — `packages/features/src/mus/server-plan.ts:236`
- **P3** `refactor` (S, high) — Five AI routes still reserve the daily allowance before parsing the body, so a malformed request costs one of five slots; only respond-handler has the fixed ordering — `src/lib/server-ai-allowance.ts:44`
- **P3** `boundary` (S, high) — packages/features server handlers read process.env for model names and budgets and hard-code 'Asia/Manila' as the timezone fallback, contradicting the global direction and the package's own injected-ports convention — `packages/features/src/mus/server-plan.ts:142`
- **P3** `boundary` (S, high) — getServiceSupabaseEnv is re-exported from the public @kayamo/db entry, weakening the boundary the service-role guard test claims — `packages/db/src/index.ts:5`
- **P3** `dead-code` (S, high) — Unread env vars, two naming schemes for the same model knobs, a VAPID name mismatch, and an exported isElevenLabsConfigured with no caller — `packages/ai/src/router.ts:81`
- **P3** `test-gap` (S, medium) — e2e Lis spec mocks /api/mus/permissions with four domains, but the client contract is a strict five-domain object since migration 0022, so the mocked response fails parse — `e2e/mus.spec.ts:15`
- **P3** `security` (S, high) — The per-user dollar budget reads agent_runs, which the user can UPDATE (any column) under RLS, so cost_usd can be zeroed; only the request-count allowance is trusted — `supabase/migrations/0001_core_schema.sql:864`
- **P3** `bug` (S, high) — Worldwide search validates its body by hand instead of Zod, and foods/parse leaves entries[].displayName and mealSlot unbounded — `packages/features/src/food/worldwide-search-server.ts:22`
- **P3** `bug` (S, high) — respond route constructs the OpenAI provider before the allowance is reserved; a missing OPENAI_API_KEY burns a user slot per turn and logs an error per request — `src/app/api/mus/respond/route.ts:46`
- **P3** `release` (S, low) — No maxDuration on the AI routes while the docs plan a Vercel Hobby deploy; the app-level 15s/30s timeouts assume the platform allows at least that — `src/app/api/mus/respond/route.ts:33`
- **P3** `dead-code` (S, high) — Synced features components call /api/foods/ocr, /api/foods/user, /api/foods/resolve and /api/notifications/subscriptions, none of which exist in this app — `packages/features/src/food/add-product-form.tsx:121`

## features-food

- **P1** `bug` (S, high) [confirmed] — Week average, week strip and presence grid drop every entry logged without a catalog food_id (worldwide search, personal foods) — `packages/features/src/food/today-table.tsx:88`
  - Fix: Add a range hook in @kayamo/offline (e.g. `useLiveFoodEntriesSince(userId, sinceLogicalDate)` over listLocalFoodEntries without the food_id filter) and use it for weekHeadline, weekStripDays and presenceCells; keep the food_id-filtered history hook for search ranking only (command-log, verify log counts). Add a today-diary test that a null-food_id entry counts in dailyKcalTotals.
  - Verifier: Verified: packages/offline/src/writes.ts:259 `listLocalFoodHistory` filters `row.food_id` truthy, and today-table.tsx:88 feeds that into weekHeadline/weekStripDays/presenceCells (l.164-205), whose `dailyKcalTotals` (week-headline.ts:54) applies no further filter, while today's list/footer uses the unfiltered `useLiveFoodEntries` (l.87). Null-food_id writes are reachable in production: `WorldwideFo
- **P1** `bug` (S, high) [confirmed] — LogSheet 'Meal → Find this food' never opens the palette: prefill event carries a string, listener expects { query } — `src/shell/desktop-shell.tsx:101`
  - Fix: Export `prefillLogPalette(query: string)` from command-log-model.ts beside openLogPalette (l.21) and call it from desktop-shell; type the event detail once. Add a shell e2e step: open Log sheet, pick Meal, type, submit, expect `[data-palette="log"]` visible with the query.
  - Verifier: Confirmed: desktop-shell.tsx:101 dispatches PREFILL_LOG_EVENT with a string detail while the sole listener in packages/features/src/food/command-log.tsx:258 reads detail?.query and bails when it is missing, so the sheet's Meal route silently closes without opening the palette; apply-proposal.ts:64 uses the correct { query } shape. The only e2e touching the path (command-palette.spec.ts:110-116) ru
- **P1** `refactor` (M, high) [reviewer-judgment] — desk.module.css (3689 lines) is a shared stylesheet mis-homed in food/, used by 14 components, with 57 unused rules — `packages/features/src/food/desk.module.css:1`
  - Fix: Delete the 57 unused rules first (364 lines). Then split by owner: `desk/desk-shell.module.css` (the 35 shared primitives, ~200 lines), `food/diary.module.css` (today-table's 437 lines, sections 1023-1600 and 3483-3560), `food/catalog.module.css` (foods-table + verify-table + the 135-line shared inspector/verify* family), `desk/planner.module.css` (todos-desk/timeline/inspector, 841-1022 and 2925-3482), `desk/movement.module.css` (gym-desk/rest/picker, 2372-2924), `screens/mus-thread.module.css` (39 exclusive classes), `desk/charts.module.css`. Do the split before the botanical pass so restyli
- **P1** `privacy` (M, high) [confirmed] — Verify overlay rewrites cached ph_core rows to confidence 1.00; entries logged afterwards claim server provenance for numbers that exist only in this browser — `packages/features/src/food/verify-model.ts:106`
  - Fix: Either ship the `verify_ph_core_food` SECURITY DEFINER migration so verification is a real catalog write, or stop patching catalog rows: keep the overlay in the Verify screen only, and when a food_entry is logged from an overlaid row stamp `resolved_via: 'user'`, keep the base confidence, and put the edit in `source_note`/`raw_input`. Add a command-log-model test that an overlaid candidate does not produce confidence 1.00 with source ph_core.
  - Verifier: Confirmed at verify-model.ts:106-121: applyOverlayToFood sets confidence '1.00' when verified while leaving source 'ph_core'; verify-table.tsx:182-190 persists the patched row via cacheFoodWithServings, hydrate-catalog.ts:13-19 re-applies the overlay for command-log/today/apply-proposal, and command-log-model.ts:124-129 logs source/resolvedVia 'ph_core' with the inflated confidence. No verify_ph_c
- **P1** `design` (S, high) [reviewer-judgment] — Diary and Verify copy is bound to the 40-dish Philippine seed catalog and becomes false for any other catalog — `packages/features/src/food/verify-model.ts:139`
  - Fix: Make verifyLede count-driven (`${verified} of ${total}`) and drop the 40/31/nine branches; make verifyQueueNote return 'nothing to verify' when the curated set is empty; rename 'PH core' to a cuisine/curated-set label sourced from one constant; decide whether /verify is a curator tool (gate it) or a user feature (make it source-agnostic: any unverified row in the user's catalog). Update the e2e assertions in verify.spec/foods.spec that look for 'PH core'.
- **P2** `bug` (S, high) — Three different initial timezone defaults; TodayTable computes 'today' in Asia/Manila until the profile loads — `packages/features/src/food/today-table.tsx:75`
- **P2** `bug` (S, high) — Diary quantity field enqueues a sync write on every blur, even unchanged, and silently ignores edits when grams is 0 — `packages/features/src/food/today-table.tsx:372`
- **P2** `a11y` (S, high) — Verify page hijacks Enter/j/k/[/]/v/'/' at window level, so Enter on any focused button verifies instead of activating it — `packages/features/src/food/verify-table.tsx:296`
- **P2** `bug` (S, medium) — Advertised ⌥1–4 meal-slot shortcut cannot fire on macOS because it matches event.key digits under Alt — `packages/features/src/food/command-log.tsx:414`
- **P2** `bug` (S, high) — Foods desk alias 'Undo' is a no-op and create-food undo leaves the row in the Dexie cache — `packages/features/src/food/foods-table.tsx:151`
- **P2** `design` (M, high) — 'New food' form sends unvalidated strings to Postgres, defaults blank macros to 0 and self-certifies confidence 1.00 with no Atwater check — `packages/features/src/food/foods-table.tsx:172`
- **P2** `boundary` (M, high) — Foods desk writes catalog rows straight to Supabase, bypassing the offline outbox; alias/merge controls are offered on rows they can never write — `packages/features/src/food/foods-table.tsx:142`
- **P2** `perf` (M, high) — Catalog hydration downloads the whole foods table on every mount (twice on /calories) and evicts with a full-table scan per cached food — `packages/features/src/food/hydrate-catalog.ts:22`
- **P2** `duplication` (M, high) — Five copies of catalog hydration, four of the history mapper, two source-coercers, three badge maps and three mark-glyph rules — `packages/features/src/food/command-log.tsx:60`
- **P2** `dead-code` (S, high) — ConversationalLog, conversational-ledger and personal-food-memory are exported but never rendered; several smaller dead branches — `packages/features/src/food/conversational-log.tsx:47`
- **P2** `a11y` (S, high) — Diary grid has no table semantics, selected day has no state attribute, and the palette combobox controls a wrapper div — `packages/features/src/food/today-table.tsx:342`
- **P2** `design` (M, high) — Two styling systems and two token vocabularies in one directory; raw buttons and three toast implementations — `packages/features/src/food/today-log.tsx:316`
- **P2** `design` (S, high) — Internal identifiers and design notes leak into user copy; Foods shows an empty catalog while loading — `packages/features/src/food/foods-table.tsx:160`
- **P2** `test-gap` (M, high) — No unit test for any of the six rendered components; e2e skips food routes on the hosted build and misses quantity edit, day pick and week math — `packages/features/src/food/today-table.tsx:73`
- **P2** `docs` (S, high) — The recorded 'web-only exception' covers three files; actual food-area divergence spans a dozen files and nine web-only modules — `docs/design/botanical/implementation-status.md:19`
- **P2** `boundary` (S, high) — LogSheet holds write logic (task create + undo) in root src/shell, and re-imports @kayamo/offline dynamically despite a static import — `src/shell/log-sheet.tsx:135`
- **P2** `design` (S, high) — Locale is hard-coded to en-PH/en-GB/en-US per call site while a locale hook exists — `packages/features/src/food/provenance.tsx:64`
- **P2** `bug` (S, high) — 'Provenance, today' card omits USDA and user-created entries and marks Brand as verified — `packages/features/src/food/today-diary.ts:143`
- **P2** `bug` (S, high) — Headline target always uses the 'rest' day type even on training days, although workouts are already loaded — `packages/features/src/food/week-headline.ts:82`
- **P3** `perf` (S, high) — waitForUserDb busy-polls every 50 ms for up to 15 s on Foods and Verify — `packages/features/src/food/wait-user-db.ts:3`
- **P3** `bug` (S, high) — Food history table shows logged_at as 'HH:MM UTC' while every other view shows the user's timezone — `packages/features/src/food/food-history.tsx:152`

## features-shell-desks

- **P1** `design` (L, high) [reviewer-judgment] — /gym and /todos are shipped in the legacy desk skin with zero botanical primitives and are absent from the botanical plan — `packages/features/src/desk/gym-desk.tsx:44`
  - Fix: Decide explicitly: either restyle GymDesk/TodosDesk onto botanical.module.css tokens and kg* primitives (SessionBar, BusyRow, LiftTable, TaskBucket, Inspector each become kgSurface cards; segmented/pills reuse kgGhost/kgAccent) as part of the redesign, or hide /gym and /todos from RAIL/Life until they are done and route Movement to a botanical placeholder. Record the choice in docs/design/botanical/implementation-status.md.
- **P2** `perf` (M, high) — Three overlapping poll loops with N+1 Dexie scans: TodosDesk 4s (per-task meta + blocked check that scans all tasks), GymDesk 4s (sets for 12 workouts + planned sets per item), GymSessionProvider 2s shell-wide with a 250ms context tick — `packages/features/src/desk/todos-desk.tsx:244`
- **P2** `refactor` (M, high) — Active gym session has two sources of truth (GymDesk page state vs GymSessionProvider), and Pause plus post-start notes are page-local and lost on navigation — `packages/features/src/desk/gym-desk.tsx:114`
- **P2** `refactor` (M, high) — todos-desk.tsx (1116 lines) bundles seven responsibilities; proposed split — `packages/features/src/desk/todos-desk.tsx:183`
- **P2** `refactor` (M, high) — gym-desk.tsx (946 lines) bundles session lifecycle, consult flow, set logging and a 300-line lift row; proposed split — `packages/features/src/desk/gym-desk.tsx:94`
- **P2** `refactor` (M, high) — screens/mus-thread.tsx (937 lines) is a web-only Lis component misfiled in the mobile screens/ directory and mixes card, list, send pipeline and composer — `packages/features/src/screens/mus-thread.tsx:56`
- **P2** `refactor` (M, high) — food/desk.module.css (3689 lines, 341 classes, 46 unused) is the shared desk stylesheet for gym, todos, charts and rest bar but is filed under food/ — `packages/features/src/food/desk.module.css:841`
- **P2** `test-gap` (M, high) — No unit tests in any directory of this area; e2e covers todos, gym and Lis with one test each and skips on hosted builds; the acceptance test pins source strings — `e2e/todos.spec.ts:8`
- **P2** `dead-code` (M, high) — The mobile shell (app-screens, chrome, day-strip, mus-habitat, copy, diagnostics, daily-loop, use-local-app-snapshot, useSecondClock) and 215 of kayamo-app.module.css's 253 classes are unreachable on web — `packages/features/src/screens/kayamo-app.module.css:1`
- **P2** `dead-code` (S, high) — Dead root shell files: use-nav-counts.ts, mus-rail-mount.tsx, placeholder.tsx and openDesktopLog have no importers — `src/shell/use-nav-counts.ts:26`
- **P2** `duplication` (S, high) — Home is implemented twice on web: DeskHome (dead) alongside BotanicalHome; desk-charts and GymPicker are dead with it — `packages/features/src/desk/desk-home.tsx:27`
- **P2** `duplication` (M, high) — Three proposal-card implementations: mus-thread LisProposalCard, dead mus/mus-proposal-card.tsx, and @kayamo/ui ProposalCard share duplicated risk/touches tables — `packages/features/src/screens/mus-thread.tsx:64`
- **P2** `bug` (S, high) — Lis rail reports 'N of 4 context areas readable' while there are five permission domains — `packages/features/src/desk/mus-rail.tsx:177`
- **P2** `design` (M, high) — The 'Context follows this screen' machinery (DeskMusPane, mus-selection, SCREEN_LABEL, moduleFromPath) feeds a shell rail that no longer exists; on /mus the panel is permanently static and its labels are stale — `packages/features/src/desk/mus-rail.tsx:153`
- **P2** `bug` (S, high) — MusThread falls back to Asia/Manila when the profile clock cannot load, contradicting the device-timezone rule every other desk surface follows — `packages/features/src/screens/mus-thread.tsx:527`
- **P3** `bug` (S, high) — Ghost block id prefix mismatch ('ghost-' vs 'ghost:') makes Delete on a proposed block a silent no-op — `packages/features/src/desk/todos-desk.tsx:861`
- **P3** `dead-code` (S, high) — Taglish locale layer is unreachable (LocaleToggle never rendered) yet still branches copy and boot logic; rules still say Philippines-first — `packages/features/src/i18n/desk-locale.ts:21`
- **P3** `duplication` (S, high) — Guest detection `userId.startsWith('guest-')` is hand-rolled in six places because isGuestId lives in root src/lib — `packages/features/src/desk/mus-rail.tsx:56`
- **P3** `a11y` (S, high) — Phone-header 'Ask Lis' button is inline-styled to 40px, below the 44px target the Settings page promises — `src/shell/desktop-shell.tsx:199`
- **P3** `design` (S, high) — Naming drift in live desk copy and identifiers after the Lis rename — `packages/features/src/desk/gym-desk.tsx:594`
- **P3** `docs` (S, high) — DiagnosticsPanel is a safe support surface (no health data) but is unreachable on web — `packages/features/src/diagnostics/diagnostics-panel.tsx:79`

## features-botanical-journey-todo-gym

- **P1** `design` (L, high) [reviewer-judgment] — Goals editor is the mobile screen design rendered inside the botanical dialog and re-skinned from the root app by CSS-module class-name substring — `packages/features/src/journey/goal-flow.tsx:42`
  - Fix: Rewrite GoalFlow as `botanical/goal-editor.tsx` composing `styles.form`, `kgAccent`/`kgGhost`, `kgEyebrow`, `.milestones` from botanical.module.css; split its three views (empty / draft / active) into small components and make the pause/release sheet a second native `<dialog>` (reusing openDialog/closeDialog) instead of a scrim div with role=dialog. Delete the `[data-botanical-goal-flow]` block from botanical.css and the `data-botanical-goal-flow` attribute. Keep the save/confirmation-UUID/persistDraft logic unchanged; it is correct.
- **P1→P2** `bug` (S, high) [confirmed] — Home 'Next up' hero is the first time block of the day regardless of the current time or whether its task is already completed — `packages/features/src/botanical/home.tsx:86`
  - Fix: When `date === today`, choose the first block with `end_min > nowMin` whose source task (if any) has no `completed_at`; otherwise the first unscheduled unfinished task; when nothing remains show the existing 'Room for what comes next' empty state. Mark completed scheduled rows in the plan list with the same `.done` treatment used in Priorities. Add a unit test for the selector with a fixed clock.
  - Verifier: Verified home.tsx:86 `const leadBlock = blocks[0] ?? null;` with listLocalTimeBlocks (schedule.ts) sorting only by start_min/created_at and never filtering by completion; useDeskClock returns nowMs but home.tsx:28 ignores it, and the hero at ~lines 280-320 renders "Start next step: <title>" for the lead task with no completed_at check, while the plan list at 335-370 renders every block without a d
- **P2** `perf` (M, high) — Every botanical link is a full-document navigation; the shell uses next/link but the screens use <a href> and window.location — `packages/features/src/botanical/goals.tsx:194`
- **P2** `perf` (M, high) — useRecords polls IndexedDB every 5 seconds; Goals issues N+1 milestone reads and Grove five whole-table reads per tick — `packages/features/src/botanical/use-records.ts:27`
- **P2** `perf` (M, high) — 316KB gym knowledge base JSON plus O(n^2) relationship inference execute at module load in the client bundle — `packages/features/src/gym/library.ts:10`
- **P2** `dead-code` (S, high) — todo/kb (compiled.json, assemble.ts, from-csv.ts) has no runtime consumer in web or mobile; its HARD planner rules never reach a prompt — `packages/features/src/todo/kb/assemble.ts:10`
- **P2** `design` (M, high) — src/app/botanical.css is a 381-line root override sheet with a shadowed second palette and its own reduced-preference blocks — `src/app/botanical.css:1`
- **P2** `boundary` (M, high) — botanical.module.css depends on global kg* classes and tokens defined only in the root app (src/app/glass.css, glass-materials.module.css) — `packages/features/src/botanical/botanical.module.css:1`
- **P2** `a11y` (M, medium) — All 41 font-size declarations in botanical.module.css are px, so the documented 200% text-size check passes without any botanical text scaling — `packages/features/src/botanical/botanical.module.css:45`
- **P2** `bug` (M, high) — Goal step ↔ Home task are linked by case-insensitive title match; completing the task never completes the milestone and the copy says the opposite — `packages/features/src/journey/goal-flow.tsx:224`
- **P2** `bug` (S, high) — applyWhatNowPick writes an AI-proposed time block and reschedules a task with no undo record and no transaction, unlike applyDayPlan — `packages/features/src/todo/apply-plan.ts:138`
- **P2** `boundary` (S, high) — Navigation IA contradicts the SoT 'five tabs only' rule: 7-item desktop rail, 4-tab phone bar without Goals or Grove — `src/shell/desktop-shell.tsx:24`
- **P2** `test-gap` (M, high) — botanical/ and journey/ have zero unit tests; pure helpers and the dialog state machine are covered only indirectly by e2e — `packages/features/src/botanical/grove.tsx:25`
- **P2** `design` (S, high) — Grove copy contradicts itself and the SoT: a 'Streak' hero that says 'It never dies' next to 'A history, not a streak to maintain', plus raw enum keys shown to users — `packages/features/src/botanical/grove.tsx:106`
- **P2** `design` (M, high) — Grove 'Life story' panel can never be populated from the web; Grove has no tree/stage visual despite the SoT and an unused `tree` icon — `packages/features/src/botanical/grove.tsx:229`
- **P2** `design` (S, high) — Internal roadmap language and device-implementation copy shown to end users — `packages/features/src/botanical/life.tsx:100`
- **P2** `bug` (S, high) — Lis rename dropped the em dash, producing a run-on sentence in the goal draft hint — `packages/features/src/journey/goal-flow.tsx:503`
- **P2** `design` (S, high) — Hard-coded 'en-PH' locale in six date/number formatters contradicts the global product direction — `packages/features/src/botanical/home.tsx:168`
- **P3** `bug` (S, medium) — closeDialog swallows the caller's onClosed when a close is already in flight, so a second click during the 180ms close animation is lost — `packages/features/src/botanical/dialog-motion.ts:51`
- **P3** `design` (S, high) — Discard handling differs between the two botanical editors: native window.confirm in TaskEditor, silent close in GoalFlow — `packages/features/src/botanical/task-editor.tsx:63`
- **P3** `duplication` (S, high) — botanical.module.css .primary/.secondary duplicate the global kgAccent/kgGhost recipes — `packages/features/src/botanical/botanical.module.css:665`
- **P3** `dead-code` (S, high) — Five BotanicalIcon names are unused, and the 'no mascot' comment contradicts the shipped Mus character assets — `packages/features/src/botanical/icons.tsx:41`
- **P3** `design` (S, high) — Three generations of assistant naming are user-visible or persisted: label 'Lis', route '/mus', data origin 'coco_confirmed' — `packages/features/src/botanical/home.tsx:180`
- **P3** `docs` (S, high) — Timetable constants comment says 07:00–21:00 but DESK_DAY_END_MIN is 22:00 — `packages/features/src/todo/timetable.ts:7`

## ai-and-mus

- **P1** `bug` (S, high) [confirmed] — Eating-disorder regexes match ordinary food-logging talk and block the model with a clinician redirect — `packages/ai/src/safety.ts:54`
  - Fix: Require a duration or intent qualifier: 'haven't eaten (in|for) N (days|a week)', 'stop eating (altogether|completely|entirely|for days)', keep 'starve myself', 'purge', 'make myself vomit' as-is. Add negative regression cases: 'i havent eaten yet, what should i have for lunch', 'i stopped eating rice', 'stopped eating late at night'. Consider a softer 'supportive_redirect' that still allows the model for the non-urgent category.
  - Verifier: Verified packages/ai/src/safety.ts lines 54 and 56: /\b(?:stop|stopped)\s+eating\b/ and /\bhave?n'?t\s+eaten\b/ have no duration or intent qualifier, and a Node check confirms "I stopped eating rice this week", "havent eaten yet, what should lunch be" and "I stopped eating after 8pm" all match. Both callers (packages/features/src/mus/respond-handler.ts:94-100 and packages/ai/src/coco-router.ts:297
- **P1** `security` (M, high) [confirmed] — Crisis classifier is English-only while the persona invites Taglish and the first market was PH — `packages/ai/src/safety.ts:30`
  - Fix: Add a Filipino/Taglish pattern bank (mamatay, magpakamatay/magpapakamatay, ayoko na mabuhay, wala nang kwenta ang buhay ko, hindi ko na kaya, sinasaktan ako ng asawa/nobyo, hindi ako makahinga, sumasakit ang dibdib) with the same normalisation, plus cases in safety.test.ts and an evals 'safety' case per language. Document that the classifier covers English + Filipino and that other languages fall through (a known gap for the global direction).
  - Verifier: Confirmed at packages/ai/src/safety.ts:30-68: all four pattern banks are English-only, rg finds no Filipino terms anywhere in packages/ai/src, and safety.test.ts has no non-English cases. No mitigation exists: persona.ts:91-92 actively invites Taglish, ph-support.ts ships PH crisis hotlines (so PH users are an explicitly served population), and neither persona.ts nor coco-router.ts adds any model-
- **P2** `design` (S, high) — start_focus is offered to the model on web but applying it always fails with a 'phone' message — `packages/ai/src/allowed-actions.ts:66`
- **P2** `dead-code` (S, high) — perm-levels.ts is a vestigial second permission model: nothing writes it, so its guard in the thread is a no-op — `packages/features/src/mus/perm-levels.ts:74`
- **P2** `dead-code` (S, high) — MusProposalCard is never rendered and duplicates the inline card logic in mus-thread.tsx — `packages/features/src/mus/mus-proposal-card.tsx:8`
- **P2** `dead-code` (S, high) — mus-faces.ts and the mascot assets survive a design decision that Lis has no character — `packages/features/src/mus/mus-faces.ts:57`
- **P2** `bug` (S, high) — parseFoodMessage's LLM branch is unreachable in production: the route passes no budget gate so completeObject always throws and the heuristic is returned — `packages/ai/src/food-parse.ts:33`
- **P2** `refactor` (M, high) — contracts.ts (761 lines) mixes five concerns and duplicates the router's request schema; split proposal — `packages/ai/src/contracts.ts:79`
- **P2** `dead-code` (S, high) — tools.ts and the tool-call contracts have no production caller; README describes agent.ts/memory.ts that do not exist — `packages/ai/src/tools.ts:13`
- **P2** `test-gap` (M, high) — apply-proposal.ts (503 lines, every Lis write path) has no unit tests — `packages/features/src/mus/apply-proposal.ts:72`
- **P2** `design` (M, medium) — Crisis footer names hotlines only for the Philippines; the product went global on Sep 14 — `packages/ai/src/ph-support.ts:47`
- **P2** `design` (S, high) — Seeded user-facing copy still says 'Coco'; the banned-copy sweep does not scan packages/db or packages/offline — `packages/db/src/seed.ts:335`
- **P2** `docs` (M, high) — Three generations of the assistant's name contradict each other across rules, docs, code identifiers, DB tables and env vars — `.cursor/rules/000-project.mdc:9`
- **P2** `security` (S, high) — ABUSE patterns only recognise intimate partners; family or household abuse passes to the model — `packages/ai/src/safety.ts:65`
- **P2** `refactor` (M, high) — Two live model entry points: openai-provider.ts calls generateObject directly, bypassing router.ts and the nutrition-key guard — `packages/ai/src/openai-provider.ts:45`
- **P2** `security` (M, high) — 'LLMs never produce nutrition numbers' is enforced only by schema key naming and prompt text; no output scan — `packages/ai/src/llm-nutrition-guard.ts:8`
- **P2** `bug` (S, high) — A hallucinated citation or malformed output discards the whole reply and reports it as an outage — `packages/ai/src/coco-router.ts:155`
- **P2** `bug` (S, high) — Model-supplied calendar dates are only regex-checked: impossible and far-past dates reach IndexedDB writes — `packages/ai/src/contracts.ts:71`
- **P2** `bug` (S, high) — complete_task ignores the per-task Lis lock (musMayEdit) that edit/delete/schedule/bulk enforce — `packages/features/src/mus/apply-proposal.ts:92`
- **P3** `bug` (S, high) — bulk_edit_tasks completes tasks without timeZone/dayStartsAt, so XP events can land on the wrong logical date — `packages/features/src/mus/apply-proposal.ts:251`
- **P3** `bug` (S, high) — Budget spend is recorded only for successful, authorised generations; rejected or retried calls are billed but invisible to the gate — `packages/ai/src/coco-router.ts:384`
- **P3** `test-gap` (S, high) — Recorded baselines never carry personaVersion, so a stale baseline is indistinguishable from a current one — `packages/ai/src/evals/baselines.ts:24`
- **P3** `dead-code` (S, high) — PhraseCache is in-memory only and no production caller passes it; the cache branch in completeObject is dead — `packages/ai/src/phrase-cache.ts:15`
- **P3** `design` (S, high) — CompanionSettings renders for demo guests and then reports 'Lis still works' although Lis is unavailable in the demo — `src/app/(shell)/settings/page.tsx:114`
- **P3** `bug` (S, high) — avoidTopics input has no per-topic length limit, so a long topic yields a 400 and an unexplained 'Could not save that' — `src/shell/companion-settings.tsx:230`
- **P3** `duplication` (S, high) — Dial scale, register list and snake_case mapping are duplicated between the settings client and server-context — `src/shell/companion-settings.tsx:22`
- **P3** `duplication` (S, high) — Default model ids and env-var fallbacks are hardcoded in six places across two naming generations — `packages/ai/src/openai-provider.ts:31`
- **P3** `refactor` (M, medium) — Two budget abstractions (AiBudgetGate and CocoBudgetStore) for one concept, bridged by a no-op — `packages/ai/src/budget.ts:9`
- **P3** `boundary` (S, medium) — Task and focus proposals are allowed even when goals_planning is not granted, so Lis may propose writes on records it cannot read — `packages/ai/src/allowed-actions.ts:57`
- **P3** `perf` (S, high) — companionProfile is sent twice per turn: in the system prompt and again inside the context JSON — `packages/ai/src/openai-provider.ts:51`
- **P3** `boundary` (S, medium) — Gym consult prompt and model call live in a root route instead of packages/features — `src/app/api/gym/consult/route.ts:30`

## offline-sync

- **P1** `bug` (M, high) [confirmed] — Food-entry Undo (restoreLocalFoodEntry) cannot succeed once the tombstone has been pushed; server tombstones are irreversible and the tombstone is pushed immediately — `packages/offline/src/writes.ts:213`
  - Fix: Pick one contract and enforce it end to end. Cheapest: give tombstone queue items a deferred `nextAttemptAt` (add an optional `{ delayMs }` to `enqueueUpsert`, use `UNDO_MS` for `deleted_at` payloads) so a restore within the undo window replaces the still-queued tombstone (same queue id) before it ships; make `restoreLocalFoodEntry` return null when no queued tombstone exists (already pushed) so the UI can say 'already deleted'. Alternative: add a server restore RPC and relax the preserve-tombstone trigger for `food_entries` only. Add a test that runs `syncUserOnce` with a pushItem that mimics
  - Verifier: Confirmed: tombstoneLocalFoodEntry (writes.ts:222-238) enqueues with nextAttemptAt=Date.now() and calls drainQueue() immediately, so the tombstone ships at once when online; restoreLocalFoodEntry (writes.ts:203-220) then enqueues a live row that the server cannot apply: food_entries_update USING requires deleted_at is null, kayamo_preserve_tombstone copies old.deleted_at forward, and the fallback 
- **P1** `refactor` (M, high) [reviewer-judgment] — Adding a sync table requires editing four hand-maintained lists plus a 26-case switch; nothing type-checks that they agree — `packages/offline/src/sync.ts:707`
  - Fix: Replace the switch with `const PUSH_HANDLERS = { food_entries: { upsert: upsertFoodEntry, local: (db) => db.food_entries }, ... } satisfies Record<SyncableTable, PushHandler>` so a new contract entry fails typecheck until handled; derive `isSyncableTable` from `BIDIRECTIONAL_SYNC_REGISTRY.some(...)`. Introduce one `TABLE_MANIFEST` in offline (name, indexes, owner column | null, kind: 'bidirectional' | 'local-only' | 'shared-cache' | 'sync-meta') from which Dexie `stores()`, USER_SCOPED_TABLES, SHARED_CACHE_TABLES and LOCAL_ONLY_TABLES are generated, and add a test asserting every `KayaMoDB` ta
- **P2** `bug` (M, medium) — Queue drain order is nextAttemptAt then primary key, so children enqueued in the same millisecond push before parents and hit FK violations (goal_milestones before goals) — `packages/offline/src/goal-plan.ts:105`
- **P2** `design` (S, high) — Server rejections are not classified: permanent errors retry 12 times over ~61 minutes before dead-letter, and pull failures have no ceiling at all — `packages/offline/src/sync.ts:231`
- **P2** `privacy` (M, high) — No user-facing way to remove offline health data from a device; sign-out keeps the account database, the legacy 'kayamo' DB is never deleted after migration, and the diagnostics report copies the auth UUID — `packages/offline/src/db.ts:1044`
- **P2** `test-gap` (M, high) — Sync orchestration, Dexie upgrade functions and React hooks have no direct tests; the drain loop's failure paths are untested — `packages/offline/src/sync.ts:196`
- **P2** `refactor` (L, high) — db.ts (1074 lines) mixes row types, Dexie schema/versions, connection lifecycle, account-scope transitions and legacy migration; split per concern — `packages/offline/src/db.ts:398`
- **P2** `bug` (M, medium) — Local write path never asserts the active account scope; only createLocalGoalPlan does, so a stale userId after an account switch writes another user's rows and orphaned queue items into the active database — `packages/offline/src/queue.ts:20`
- **P2** `perf` (S, high) — Every window focus, visibilitychange and TOKEN_REFRESHED triggers a full 26-table pull (26 sequential GETs) with no minimum interval — `packages/offline/src/sync.ts:665`
- **P2** `bug` (S, high) — logical-date defaults to 'Asia/Manila' (and features seed the same default before profile hydration), so non-Philippine users can be bucketed to the wrong logical day — `packages/offline/src/logical-date.ts:7`
- **P2** `dead-code` (M, high) — A second, legacy merge API (26 mergeRemote* functions) plus logWorkout, useLocalAppSnapshot, useLivePlanningSnapshot and ~140 barrel exports have no production consumer — `packages/offline/src/writes.ts:263`
- **P2** `refactor` (M, high) — Domain modules import the sync orchestrator to fire `void drainQueue()` (55 unguarded call sites), inverting the layering and forcing 12 of 23 test files to mock ./sync — `packages/offline/src/writes.ts:126`
- **P3** `perf` (M, high) — useLiveFoodHistory is mounted by seven components, each re-reading and sorting the entire food_entries table on every write; date queries also lack a compound index — `packages/offline/src/hooks.ts:62`
- **P3** `a11y` (S, high) — SyncStatusBar is a bare span with terse labels, no live region and no remedy affordance — `packages/features/src/sync/sync-status-bar.tsx:27`
- **P3** `design` (S, high) — notifySyncStatus fires without refreshing the snapshot, and scheduleDrain/scheduleSync share one timer and clobber each other — `packages/offline/src/sync.ts:379`
- **P3** `bug` (S, medium) — v14 upgrade assigns userId '' to ownerless queue rows, which then count as pending forever but are never due for any user — `packages/offline/src/db.ts:553`
- **P3** `boundary` (S, high) — Several child-keyed reads and writes skip the owner check, relying solely on per-account database isolation — `packages/offline/src/training.ts:211`
- **P3** `duplication` (S, high) — goal-plan rebuilds SyncQueueItem by hand and newId/nowIso helpers are copied into ten modules — `packages/offline/src/goal-plan.ts:93`
- **P3** `docs` (S, high) — README and rules lag the code: 'consumed by apps/*', unused ./logical-date subpath, Manila default undocumented, classification lists described as complete — `packages/offline/README.md:1`

## db-and-migrations

- **P0→P1** `release` (M, medium) [confirmed] — Production schema is behind the checked-in migrations (0018 unapplied, 0021/0022 unrecorded) and there is no ledger of what was applied — `supabase/migrations/0022_lis_identity_domain.sql:17`
  - Fix: Before deploying this branch: `supabase link` to the hosted project, `supabase migration list` to diff `supabase_migrations.schema_migrations` against supabase/migrations/, apply 0018 (if still missing), 0021 and 0022 with `supabase db push`, and record the applied set in docs/releases/. Add a preflight (script or CI job with a read-only prod credential, or a manual checklist item in release-candidate.md) that fails when the directory has migrations the target lacks. Retire scripts/web-ai-allowance.mjs once 0020 is tracked by the CLI ledger.
  - Verifier: Confirmed: docs/audits/2026-09-15-lis-qa.md:151-153 records 0018 unapplied in the hosted project, no later doc (docs/releases/ has only 2026-09-10-web.md covering 0020; docs/inventory-2026-08.md:91 admits the live project was never queried) records applying 0018/0021/0022, supabase/migrations/meta/_journal.json is a Drizzle file journal not an applied ledger, CI only replays migrations against an 
- **P1** `release` (S, high) [confirmed] — Two migration runners with divergent bookkeeping; the Drizzle journal is stale and has no snapshots — `supabase/migrations/meta/_journal.json:131`
  - Fix: Pick the Supabase CLI as the only runner (CI already does). Delete supabase/migrations/meta/, remove the `generate`/`migrate` scripts (keep drizzle-kit only if you adopt `drizzle-kit check`/introspect as a drift test against the local replayed DB), and rewrite README.md's 'Local migrations' section around `supabase db reset` locally and `supabase db push` for deploys. Until then, `drizzle-kit migrate` silently skips 0020–0022 and `drizzle-kit generate` would emit a full-schema duplicate migration.
  - Verifier: Verified every factual claim: supabase/migrations/meta/_journal.json ends at idx 18 / 0019_sync_sequence while 0020–0022 exist (0020/0022 were added in commit 9db7dc3 without touching meta/), meta/ has no snapshot file, packages/db/package.json still exposes drizzle-kit generate/migrate with out pointing at supabase/migrations, CI applies via `supabase db reset` (ci.yml:43), and both packages/db/R
- **P1→P2** `security` (S, high) [confirmed] — seed.ts and ph-core.ts write to whatever DATABASE_URL names, and the root .env.local points it at production — `packages/db/src/seed.ts:535`
  - Fix: Add `assertDisposableDatabaseUrl(url)` in packages/db/src/drizzle.ts that allows 127.0.0.1/localhost/[::1] and otherwise requires `KAYAMO_ALLOW_REMOTE_DB=1` and prints the hostname; call it from `createDrizzle`. Rename the production connection string in .env.local to something scripts never read by default (e.g. `KAYAMO_PROD_DATABASE_URL`) and have web-ai-allowance.mjs read that. Reuse the loopback check already written in scripts/github-supabase-env.mjs:26.
  - Verifier: Confirmed at the cited lines: drizzle.ts requireDatabaseUrl only checks presence, seed.ts:535 and ph-core.ts:16 open whatever DATABASE_URL names, seed.ts:781 deletes recipe_ingredients for the seeded recipe and ph-core upserts foods overwriting kcal/macros, and .env.local's DATABASE_URL host is db.vfprugxigejdsyvipipf.supabase.co (which web-ai-allowance.mjs:8 requires by design, so pointing the sh
- **P1** `refactor` (M, high) [reviewer-judgment] — LWW upsert and tombstone logic is copy-pasted ~16 times with three competing generic variants and one behavioural divergence — `packages/db/src/queries/journey.ts:92`
  - Fix: One `lwwUpsert(client, spec: BidirectionalSyncSpec, row, { strip?: string[] })` and one `tombstone(client, spec, { id|user_id, updatedAt })` in queries/lww.ts, driven by BIDIRECTIONAL_SYNC_CONTRACT (ownerColumn, stableKeyColumn, tombstones). Each queries/<domain>.ts keeps only its list/read helpers and a `toInsert` normalizer. Delete the three private generics and the eight tombstone copies; make 'empty RETURNING is failure' the single rule. This should land before the wider refactor because every other package calls these helpers.
- **P2** `refactor` (S, medium) — Browser, cookie and service Supabase factories are untyped, so every direct .from() in features is any-typed — `packages/db/src/client.ts:12`
- **P2** `duplication` (S, high) — Catalog seed data (scripture, stages, achievements, cosmetics) is defined in both SQL migrations and TypeScript — `packages/db/src/scripture-data.ts:2`
- **P2** `bug` (M, medium) — Upsert conflicts on a non-id live unique index return row:null and the offline push silently drops the write — `packages/db/src/queries/weight-logs.ts:104`
- **P2** `bug` (M, high) — Drizzle schema has drifted from the SQL it claims to mirror (~35 constraints/indexes missing or renamed) — `packages/db/src/schema/agent.ts:41`
- **P2** `design` (M, high) — Philippines-first defaults are baked into column defaults, the signup trigger and the logical-date function; nothing writes a real timezone — `packages/db/src/schema/profiles.ts:16`
- **P2** `test-gap` (M, high) — RLS suite leaves 19 of 46 tables without a cross-user assertion; lis_companion_profile and the mus_may_read readers have no test at all — `packages/db/src/rls.test.ts:86`
- **P3** `security` (S, medium) — meal_templates and off_contribute_requests never revoke DML from anon; off_contribute_requests grants UPDATE with no UPDATE policy — `supabase/migrations/0005_meal_templates.sql:37`
- **P3** `duplication` (S, high) — Enum vocabularies are duplicated between constants.ts and hand-written CHECK strings — `packages/db/src/schema/foods.ts:228`
- **P3** `dead-code` (S, high) — Three env-file parsers, a redundant loadRootEnv() in next.config.ts, and parent-directory .env.local lookups — `packages/db/src/load-root-env.ts:9`
- **P3** `bug` (S, high) — Local Supabase auth config only allows redirects to ports 3000/3001 while the web app runs on 3002; configured seed.sql does not exist — `supabase/config.toml:163`
- **P3** `dead-code` (S, high) — run-disposable-db-tests.mjs is welded to one dated temp project and will not run again — `scripts/run-disposable-db-tests.mjs:6`
- **P3** `docs` (S, high) — Data-layer docs and cursor rules describe commands, layout and RLS rules that no longer match the code — `packages/db/README.md:64`
- **P3** `design` (S, high) — ph_core plumbing is sound but hard-codes a single curated source and runs its upsert without a transaction — `packages/db/src/ph-core.ts:20`
- **P3** `refactor` (S, high) — Companion settings are split across two naming eras (mus_context_permissions vs lis_companion_profile) with inconsistent write conventions — `packages/db/src/queries/lis-companion.ts:49`
- **P3** `test-gap` (S, medium) — write-schemas.ts covers 2 of 26 synced tables; the module name promises a layer that does not exist — `packages/db/src/write-schemas.ts:31`
- **P3** `perf` (S, medium) — Search fallbacks load the whole catalog into memory and ilike patterns are not escaped — `packages/db/src/queries/foods.ts:259`

## food-core-domain

- **P1→P2** `release` (M, high) [confirmed] — Calorie-floor/target engine is never executed on the web: no guidance route, zero callers of generateNutritionTarget/estimateTdee, yet the Health screen tells users to 'Set up targets' — `packages/core/src/targets.ts:49`
  - Fix: Before ship, either (a) port mobile's src/lib/guidance-server.ts into packages/features/src/guidance/guidance-server.ts (shared, like worldwide-search-server.ts) and add src/app/api/guidance/route.ts that computes estimateTdee + generateNutritionTarget from confirmed weight/intake rows and writes expenditure_estimates/targets, or (b) hide the 'Weight & guidance' / 'Set up targets' UI on web until (a) lands. Add an e2e that a web-only account with a complete profile and one weight log gets a target >= floor.
  - Verifier: Confirmed the core claim: generateNutritionTarget/estimateTdee/insertNutritionTargets have zero callers outside packages/core and packages/db, src/app/api has no guidance route (mobile has src/app/api/guidance/{route,expenditure,targets} + src/lib/guidance-server.ts), and the web only reads targets via listEffectiveNutritionTargets (server-context.ts, desk-home.tsx, today-table.tsx), so a web-only
- **P1** `release` (S, high) [confirmed] — Three nutrition-correctness fixes (per-100g USDA, no fabricated zero macros, independent providers) are uncommitted in both repos; production still runs the old code — `packages/food/src/sources/usda.ts:208`
  - Fix: Commit these in kayamo-mobile first (source of truth), run ../sync-packages.sh, then commit here so the branch and PR to feat/web carry them; do not ship kayamo.fit from a commit that predates them. Keep the two new test files.
  - Verifier: Confirmed: git status in both kayamo-web and ../kayamo-mobile shows the same 5 modified + 2 untracked files under packages/food, and the last commit touching usda.ts in either repo is the original 932846b. HEAD usda.ts (lines 208-212) still calls perServingToPer100g on Branded foods whose FDC foodNutrients are already per 100 g, so the fixture's 165 kcal / 20.42 g protein would ship as 58.099 / 7.
- **P1** `bug` (M, medium) [confirmed] — Cached usda_fdc Branded rows written before the per-100g fix are divided by serving size; no audit exists although docs require one before rollout — `packages/food/src/cache.ts:76`
  - Fix: Add scripts/audit-usda-cache.ts: select foods where source='usda_fdc', compute `atwaterDelta`-style check (validate.ts already has the formula) and/or compare kcal against a fresh getUsdaFood(fdcId) once the key works; report rows where kcal < 4*protein+4*carbs+9*fat*0.5 or delta > 0.05, and correct them with a source_note. Gate the ship on it, or delete usda_fdc cache rows if the 403 means none exist in prod (verify count first).
  - Verifier: Confirmed: packages/food/src/cache.ts:76-81 returns the existing row forever and the uncommitted usda.ts diff shows the committed code did divide Branded foodNutrients by serving grams (perServingToPer100g), so any usda_fdc rows cached under the old adapter are wrong per-100g values with no source_note or migration distinguishing them; no script, migration, or release-gate doc (release-candidate.m
- **P2** `test-gap` (M, high) — confidence.ts, daily-nutrition.ts, energy-balance.ts, portion.ts, numeric.ts, user-alias.ts, http.ts and catalog*.ts have no dedicated tests; confidence levels drive what users see as VERIFIED/KNOWN and the calorie ranges — `packages/food/src/confidence.ts:19`
- **P2** `bug` (S, high) — parseFoodQuery treats any text whose digits total 8–14 as a barcode, emptying results for ordinary multi-number queries — `packages/food/src/query-parse.ts:110`
- **P2** `bug` (S, high) — resolvePortion silently multiplies by the default serving when the user's unit matches no serving, and reports the mismatched unit as if honoured — `packages/food/src/portion.ts:90`
- **P2** `boundary` (S, high) — resolveFood still carries an LLM rung whose LlmEstimate type accepts per100g from a model, contradicting 000-project.mdc and llm-nutrition-guard; .cursor rule 400 contradicts rule 000 on this — `packages/food/src/resolve.ts:42`
- **P2** `design` (S, high) — Meal-slot labels default to Taglish (Almusal/Tanghalian/Meryenda/Hapunan) while the desk default locale is English; several web call sites omit the locale — `packages/food/src/meal-slot.ts:9`
- **P2** `refactor` (M, high) — @kayamo/food has six overlapping entry points and its root barrel ships server-only code (USDA/OFF fetchers, Supabase cache, HourlyLimiter) into 'use client' components — `packages/food/src/index.ts:124`
- **P2** `release` (S, high) — Source HTTP layer has no timeout/retry of its own and treats 401/403/429 like any failure, so an invalid USDA key is re-tried on every search and burns budget — `packages/food/src/sources/http.ts:16`
- **P3** `duplication` (M, high) — Three candidate-dedupe implementations, two alias-match passes per query, and four logical-date helpers duplicated across food and core — `packages/food/src/resolve.ts:197`
- **P3** `perf` (M, medium) — In-memory catalog search is O(N x aliases x tokens) trigram work done twice per keystroke and rebuilds the query trigram set for every candidate; N grows with every cached USDA/OFF food — `packages/food/src/trigram.ts:13`
- **P3** `security` (S, high) — USDA API key is sent in the URL query string instead of the X-Api-Key header, so it appears in any thrown fetch error or upstream log — `packages/food/src/sources/usda.ts:236`
- **P3** `release` (S, medium) — Open Food Facts defaults point at the old domain and exceed OFF's published search rate: User-Agent contact@kayamo.ph, 1000/hr default limiter, legacy cgi/search.pl endpoint — `packages/food/src/sources/off.ts:17`
- **P3** `refactor` (S, high) — packages/core/src/index.ts is a barrel that also contains a 140-line buildDailyContext implementation; ~35 exported core functions have no web caller — `packages/core/src/index.ts:262`
- **P3** `dead-code` (S, high) — Mobile-only food code with no web caller: network rung, query cache, Supabase catalog/canonical store, ph-core admin IO/diff, getUsdaFood, yamlPhCoreCatalog — `packages/food/src/catalog-supabase.ts:76`
- **P3** `docs` (S, high) — Nutrition rules still encode Philippines-first behaviour that the code implements: PH units sorted first, en-PH number formatting, PHP-only money, and a cascade rule that disagrees with score priorities — `.cursor/rules/400-nutrition-domain.mdc:26`
- **P3** `duplication` (S, high) — build-demo-catalog.ts re-implements the ph-core row mapping with different default-serving semantics and only warns on validation errors — `scripts/build-demo-catalog.ts:82`
- **P3** `bug` (S, high) — foodParseSchema leaves LLM-emitted quantity and quantity_multiplier unbounded, so a hallucinated 1000 or negative value flows into grams — `packages/food/src/food-parse.ts:22`
- **P3** `test-gap` (S, medium) — Label OCR drafts are not sanity-checked against Atwater before the user is asked to confirm, so a misread digit (e.g. 5390 kcal) reaches the confirm sheet — `packages/food/src/label-ocr.ts:124`
- **P3** `dead-code` (S, medium) — data/exercises/ contains only a README saying not to use it; data/ph-core/prices.yaml is NCR PHP pricing with no web consumer — `data/exercises/README.md:1`

## design-system-and-css

- **P1** `boundary` (L, high) [confirmed] — packages/features and packages/ui stylesheets depend on glass tokens (--ink2, --stroke2, --accent, --field, --glass) and kg* global classes that are defined only in root src/app/glass.css and glass-materials.module.css; packages import from the root app in violation of the boundary and mobile defines none of them — `packages/features/src/botanical/botanical.module.css:1` (dedups root-app-5)
  - Fix: Decide before the refactor, not after: either (a) remove `packages/features` and `packages/ui` from SHARED in sync-packages.sh and declare them web-owned (mobile keeps its own copies), or (b) keep sharing components but move every web stylesheet out of the synced tree (e.g. `src/styles/features/*.module.css` passed in via className props, or a `packages/features-web` workspace). In both cases move the token definitions the packages read (--ink*, --stroke*, --glass*, --accent*, --field, --track, --shadow*) into packages/ui/src/tokens.css so the dependency arrow points package -> package, never 
  - Verifier: Confirmed: --ink2/--stroke2/--field/--accent/--glass are defined only in src/app/glass.css (lines 15-29, dark/forced-colors overrides later) and not in packages/ui/src/tokens.css; packages/features CSS references them 315 times and features tsx hardcodes kg* global class names (mobile has zero kg* or --ink2/--stroke2/--field definitions; mobile tokens.css only defines --glass). Header of botanical
- **P1** `refactor` (M, high) [reviewer-judgment] — Six token sources (ui tokens.css, botanical.css, glass.css bridge, kayamo-app.module.css --km-*, login-form, web-motion) redefine the same concepts with three competing values; botanical.css's palette is dead because glass.css re-declares every property on the identical selector, and only day/night is a real runtime theme axis — `src/app/botanical.css:2` (dedups root-app-6)
  - Fix: One token file: packages/ui/src/tokens.css holding (1) the glass semantic set (--bg0/--glass/--glass-strong/--stroke/--stroke2/--ink/--ink2/--ink3/--accent/--accent-text/--accent-ink/--field/--track/--shadow/--shadow-deep/--danger/--danger-text, motion durations/easings, radii, type scale) in :root and html[data-kayamo-theme='night'], (2) the --color-* aliases as a temporary bridge with a TODO per consumer, (3) every preference media block. Delete the tokens.css Option A values, the botanical.css lines 1-52, and glass.css lines 8-104 and 375-406 once moved. Rename --km-* bridging to live only 
- **P1→P2** `bug` (S, high) [confirmed] — Press feedback fires twice and every module-level button/link transition is dead: glass.css and web-motion.css both own :active at (0,2,2) specificity — `src/app/glass.css:434`
  - Fix: Delete glass.css:430-450 so web-motion.css is the only press contract. In web-motion.css wrap the element selectors in `:where(...)` (zero specificity) so a module class can still override transition-property or scale. Add a motion.spec assertion that `getComputedStyle(el).transform === 'none'` while pressed (only `scale` may change) and that `.nav a` hover transitions background.
  - Verifier: Confirmed: src/app/globals.css imports glass.css then @kayamo/ui/web-motion.css; glass.css:434-440 sets `transform: scale(0.98)` and `transition: transform` while packages/ui/src/web-motion.css:9-21 uses the identical `html[data-kayamo-web] :is(...)` selector shape, so its `transition-property: opacity, scale` wins the cascade (transform snaps untransitioned) while both `transform: scale(0.98)` an
- **P1→P2** `bug` (S, high) [confirmed] — Global :focus-visible rule sets border-radius: 4px, so every pill and card snaps square on keyboard focus — `src/app/glass.css:281`
  - Fix: Remove `border-radius: 4px` from the focus rule; if a rounded ring is wanted use `outline-offset` plus `border-radius: inherit` is unnecessary (outline follows border-radius in modern engines). Add one e2e check: focus `.tabAdd` via keyboard and assert computed borderRadius stays 999px.
  - Verifier: Confirmed at /Users/rovs/Documents/KayaMo/kayamo-web/src/app/glass.css:281-285: `html[data-kayamo-web] :focus-visible` sets `border-radius: 4px` (specificity 0,2,1) and glass.css is imported last in globals.css, so it overrides single-class module radii such as `.kgAccent` (glass.css:145, 999px, used in 8 tsx files), `.tabAdd`/`.chip` (src/shell/glass-shell.module.css:307/438, 999px); the only oth
- **P2** `design` (M, high) — Visual inconsistency inventory: 30+ radii, 6 focus-ring variants, 17 font stacks with four undefined faces, 4 shadow vocabularies, 15 breakpoints — `packages/ui/src/tokens.css:26`
- **P2** `a11y` (M, high) — No forced-colors handling anywhere; glass draws edges, dividers, checkbox rings and switch tracks with box-shadow and border:0, which Windows High Contrast removes — `packages/features/src/botanical/botanical.module.css:316`
- **P2** `a11y` (M, high) — Large-text coverage is vacuous: the glass system is px-only and botanical.css pins html font-size to 16px, so the e2e 200% test scales nothing — `src/app/botanical.css:36`
- **P2** `duplication` (S, high) — Reduced-motion, reduced-transparency, prefers-contrast and no-blur fallbacks are each declared 2-4 times with divergent values — `src/app/glass.css:292`
- **P2** `refactor` (M, high) — packages/ui primitives are bypassed (211 raw <button> vs 21 <Button> in features) because Button is a mobile full-width control; three toast and three sheet/dialog implementations coexist — `packages/ui/src/components/Button.tsx:22`
- **P2** `refactor` (L, high) — desk.module.css (3,689 lines, 342 classes) is one stylesheet for five routes in two domains, imported from 12 files across food/ and desk/ — `packages/features/src/food/desk.module.css:1`
- **P2** `test-gap` (S, high) — tokens.test.ts pins palette values nothing renders and that mobile has abandoned; no test guards the cascade bugs — `packages/ui/src/tokens.test.ts:8`
- **P2** `docs` (S, high) — Design docs describe a botanical, opaque, textured UI while the code and tests render Liquid Glass; no doc records the glass pass — `design-qa.md:39`
- **P2** `refactor` (S, medium) — The 'pipeline strips backdrop-filter' claim that shapes glass-materials.module.css and four module headers is unverified and internally contradicted — `src/app/glass-materials.module.css:3`
- **P2** `boundary` (M, high) — Root shell owns a second log-entry feature (LogSheet) with its own copy, chips and task writes, duplicating CommandLog in packages/features with a different look — `src/shell/log-sheet.tsx:4`
- **P2** `design` (L, high) — Every shell route mixes systems: glass chrome around desk/mobile/--color-* content; /goals stacks four layers; Landing and Login differ — `src/app/(shell)/goals/page.tsx:5`
- **P2** `boundary` (M, high) — src/app/botanical.css overrides package CSS-module internals from root via 48 [class*=] substring selectors — `src/app/botanical.css:98`
- **P2** `perf` (M, medium) — kayamo-app.module.css (4,817 lines, 255 classes, 132 unused on web) ships for one dialog and is likely pulled into every shell route via the features barrel — `packages/features/src/journey/goal-flow.tsx:42`
- **P2** `dead-code` (S, high) — Dead stylesheet, 42 of 49 dead shell classes, three dead components, two dead assets, a hidden wash div and two unused ui primitives — `src/shell/botanical-shell.module.css:1`
- **P3** `refactor` (M, medium) — Tailwind is preflight plus @theme utilities for six primitives; on web routes utilities appear in four files, yet it is listed as a stack pillar and scans the whole repo — `packages/config/tailwind.css:1`
- **P3** `boundary` (S, high) — Packages depend on root-defined --font-manrope and the root data-kayamo-web attribute — `packages/features/src/food/desk.module.css:1`
- **P3** `bug` (S, high) — Shared primitives hard-code the en-PH locale and a fixed dialog id — `packages/ui/src/components/NumberDisplay.tsx:17`
- **P3** `design` (S, high) — Disabled buttons show a wait cursor globally and the proposal colour was collapsed into the accent — `src/app/botanical.css:63`

## tests-ci-tooling-release

- **P0→P1** `release` (S, high) [confirmed] — Committed HEAD imports files git has never tracked (site-metadata, glass.css, glass-materials/glass-shell modules, web-motion.css, dialog-motion, timeline-physics, default-clock, worldwide-food-search + worldwide-search-server + /api/foods/worldwide, load-root-env, check-security-dependencies); a clean checkout of codex/botanical-liquid-glass fails typecheck, build and test collection — `src/app/layout.tsx:4` (dedups root-app-1, features-botanical-journey-todo-gym-1, design-system-and-css-1, ai-and-mus-1, api-routes-3, features-food-3)
  - Fix: Before any other work: `git add src/lib/site-metadata.ts src/lib/site-metadata.test.ts src/app/robots.ts src/app/sitemap.ts src/app/glass.css src/app/glass-materials.module.css src/shell/glass-shell.module.css src/app/global-positioning.test.ts packages/ui/src/web-motion.css packages/ui/src/use-web-sheet-motion.ts packages/features/src/botanical/dialog-motion.ts packages/features/src/todo/timeline-physics.ts packages/features/src/todo/timeline-physics.test.ts packages/features/src/food/default-clock.ts packages/features/src/food/worldwide-food-search.tsx packages/features/src/food/worldwide-fo
  - Verifier: Confirmed: HEAD:src/app/layout.tsx line 4 imports '@/lib/site-metadata' and HEAD:src/app/globals.css imports ./glass.css and @kayamo/ui/web-motion.css, yet git ls-files shows all 12 spot-checked modules (site-metadata, glass.css, glass-materials/glass-shell modules, web-motion.css, use-web-sheet-motion, dialog-motion, timeline-physics, default-clock, worldwide-food-search, worldwide-search-server,
- **P1→P2** `release` (M, high) [confirmed] — Working tree: 88 entries mix five unrelated changesets, the release-defining docs are untracked, and the staged set is not self-consistent — `.gitignore:15`
  - Fix: Commit plan (in order): (1) `fix: commit files HEAD depends on` — the P0 list plus packages/ai/src/evals/load-root-env.ts. (2) `feat(web): botanical redesign — motion, glass, goal-plan recovery, a11y specs` — glass*.css, web-motion.css, use-web-sheet-motion.ts, Sheet/Button/ProposalCard, dialog-motion, timeline-physics, default-clock, goal-plan*, offline/index.ts, e2e/{accessibility,goal-recovery,motion,command-palette,foods}.spec.ts, @axe-core/playwright, docs/design/botanical/*.md, design-qa.md moved to docs/design/botanical/qa.md, .gitignore with `output/` (not `output/imagegen/`). (3) `fea
  - Verifier: Verified with `git status --short`: 89 entries (41 tracked modified/staged, 48 untracked — the finding's 42/56 split is slightly off but the substance is right). The staged .gitignore hunk adds `output/imagegen/`, the 1.9 MB comparison-v5.png is staged, release docs (global-product-direction, legal drafts, security-remediation, tutorial-application, CLAUDE.md) are untracked, docs/audits/2026-09-15
- **P1** `release` (S, high) [confirmed] — Branch model is inconsistent: production is origin/main, the PR target feat/web is 12 commits behind main, and no CI has ever run on this branch — `.github/workflows/ci.yml:2`
  - Fix: Decide once which branch Vercel promotes (evidence says main). Either retarget the PR at main, or fast-forward feat/web to main before opening the PR so the diff is only the redesign. Push the branch as soon as the P0 dangling-file fix lands so CI produces a real signal; do not rely on the locally recorded 132/132 numbers, which came from `next dev` on a dirty tree. Add `concurrency: { group: ci-${{ github.ref }}, cancel-in-progress: true }` and restrict `on:` to `pull_request` plus `push: branches: [main]` to stop double runs.
  - Verifier: Verified every factual claim: ci.yml:2 is `on: [push, pull_request]` with no concurrency block; `origin/HEAD` -> origin/feat/web (last commit Sep 1, a1499d1) while origin/main (Sep 10, e7326d8) is the recorded production commit and 12 commits ahead of feat/web; HEAD is 8 ahead of main, 20 ahead of feat/web; `git ls-remote` shows codex/botanical-liquid-glass has never been pushed, and the newest `g
- **P1** `test-gap` (M, high) [reviewer-judgment] — CI end-to-end tests run only against the Turbopack dev server; the production webpack build is never served or smoke-tested — `playwright.config.ts:20`
  - Fix: Add a third CI job `production-smoke`: `pnpm build` -> `pnpm start` -> `PLAYWRIGHT_BASE_URL=http://localhost:3002 pnpm exec playwright test --project=chromium` for the specs that are not gated on local sign-in (smoke, entry-pages minus mocked auth, launch-metadata, botanical*, web-release, goal-recovery, accessibility). For authenticated coverage against the production build, replace the NODE_ENV gate with an explicit `E2E_LOCAL_LOGIN=1` env read in local-dev.ts that CI sets only for the disposable-DB job, or create the session once in a Playwright `setup` project and reuse `storageState`.
- **P1** `docs` (M, high) [reviewer-judgment] — Three contradictory product truths and monorepo-era rules leave the refactor without a single source; the global-direction doc that overrides them is untracked — `.cursor/rules/000-project.mdc:8`
  - Fix: Create docs/archive/ and `git mv` into it: docs/build/ (all), docs/build-bundles.md, docs/inventory-2026-08.md, docs/inventory/, docs/design/desktop-handoff/, docs/design/claude-brief-phase-0-1.md, docs/design/qa/, qa-round-2/, qa-round-3/, and kayamo-build-prompts/. Move Mus_Build_Source_of_Truth.md to docs/product/source-of-truth.md with a one-paragraph preface listing what docs/global-product-direction.md and the Lis rename supersede. Commit docs/global-product-direction.md. Replace .cursor/rules with the mobile repo's standalone versions adapted for web (root `src/` = routes/composition on
- **P1** `release` (M, high) [confirmed] — Consolidated release gates: 14 open items across six docs, two of them (missing migration 0018, no legal routes) not in the known-gate list — `docs/design/botanical/release-candidate.md:87`
  - Fix: Single checklist, owner-action vs code-action. OWNER: (1) obtain a working USDA_FDC_API_KEY and set it locally + Vercel; (2) confirm operator legal name/jurisdiction, approve privacy/terms/accessibility drafts after professional review; (3) verify kayamo.fit in Search Console, submit sitemap after deploy; (4) confirm Vercel keeps deployment e7326d8 for rollback and note backup retention/RTO/RPO; (5) set Vercel env per the rewritten .env.example (SUPABASE_SERVICE_ROLE_KEY, OPENAI_API_KEY, MODEL_*, WEB_AI_DAILY_REQUEST_LIMIT, USDA, OFF_USER_AGENT); (6) confirm Supabase Auth Site URL/redirects pe
  - Verifier: Verified release-candidate.md:87 "Outstanding release gates" lists only four items (push, preview smoke/approval, zoom/screen-reader, rollback retention); docs/releases/ contains only 2026-09-10-web.md, and no consolidated checklist exists. The two "unknown" gates are real and unmitigated: docs/audits/2026-09-15-lis-qa.md:135-150 records that supabase/migrations/0018_mus_context_permissions.sql is
- **P1→P2** `release` (S, high) [confirmed] — packages/config still pins next/eslint-config-next 16.3.1, so the lockfile carries two Next versions and the vulnerable sharp 0.35.3 alongside the patched one — `packages/config/package.json:27`
  - Fix: In kayamo-mobile first, change packages/config to declare `next` and `eslint-config-next` as `peerDependencies` (`">=16.3.3"`) instead of devDependencies, or bump both to 16.3.3; sync to web; run `pnpm install` and confirm `grep -c '^  next@' pnpm-lock.yaml` is 1. This also halves the SWC binary download in CI.
  - Verifier: Confirmed: packages/config/package.json:27-28 (and the canonical ../kayamo-mobile copy) pin next/eslint-config-next 16.3.1 while root uses 16.3.3; pnpm-lock.yaml and node_modules/.pnpm carry both next versions, sharp 0.35.3 and 0.35.4, and every @next/swc-* binary twice (32 entries), and the `next@16.3.3>sharp` override plus scripts/check-security-dependencies.mjs only cover the root copy. Additio
- **P2** `test-gap` (S, high) — Authenticated e2e specs silently skip when the local sign-in button is absent, so a run against a production server passes green with 12 tests doing nothing — `e2e/command-palette.spec.ts:14`
- **P2** `test-gap` (S, high) — No trace, report or artifact is ever captured on CI e2e failure (trace on-first-retry with retries undefined, no reporter, no upload step) — `playwright.config.ts:18`
- **P2** `perf` (M, high) — CI database-integration job runs 3 browsers serially with workers=1 under a 25-minute cap that the current suite has never been measured against — `.github/workflows/ci.yml:25`
- **P2** `duplication` (M, high) — e2e suite repeats the sign-in preamble 10 times, the demo entry 6 times, and raw IndexedDB open/transaction boilerplate 9 times; helpers hard-code Asia/Manila — `e2e/helpers/food-entries.ts:9`
- **P2** `test-gap` (L, high) — Critical flows with no e2e coverage: sign-out, a Lis chat round-trip, the auth callback routes, settings/profile/targets, data export/delete, and any client-to-Supabase sync despite CI having a live database — `e2e/mus.spec.ts:11`
- **P2** `release` (M, high) — Hosted smoke exists as a script but never runs anywhere, and there is no preview-deploy smoke, axe/Lighthouse on preview, or bundle budget — `package.json:22`
- **P2** `dead-code` (S, high) — 3.6 MB of mascot PNGs and the newly staged mus-*.webp/prompts are unreferenced in the web runtime; the Lis rename commit removed the mascot the staged assets add — `public/mus-neutral.png`
- **P2** `dead-code` (S, high) — Six stray top-level directories and one tracked scaffold duplicate data that already lives elsewhere (about 25 MB) — `gym_knowledge_base_v1/README.md`
- **P2** `refactor` (M, high) — 38 MB of design screenshots and HTML prototypes are tracked in git and grow with every QA iteration — `docs/design/botanical/evidence/comparison-v5.png`
- **P2** `dead-code` (S, high) — Scripts: three one-off or never-executed scripts, one duplicate, and a README that lists 4 of 13 and makes a false claim — `scripts/README.md:4`
- **P2** `security` (S, medium) — `pnpm ph-core:build` and `scripts/seed.ts` write to whatever DATABASE_URL .env.local supplies — in this repo that is the production database — with no target confirmation — `scripts/build-ph-core.ts:31`
- **P2** `security` (S, high) — The desktop repo's .env.local carries production service-role, database, VAPID private and OAuth secrets, most of which no code in this repo reads — `.env.example:1`
- **P2** `release` (S, high) — Node version parity is undefined: CI on 22, developer on 20, @types/node ^20, no engines or .nvmrc — `package.json:50`
- **P2** `refactor` (S, high) — Security dependency check is an exact-version pin masquerading as a test, and the overrides use version-scoped selectors that silently stop matching after any bump — `scripts/check-security-dependencies.mjs:18`
- **P3** `docs` (S, high) — next-env.d.ts is tracked and churns between dev and build; Next 16 documentation says to gitignore it — `next-env.d.ts:3`
- **P3** `test-gap` (S, high) — Generated artifacts (gym/todo compiled.json, public/demo-catalog.json) have no drift check in CI even though two of the builders already implement --check — `scripts/build-gym-kb.ts:21`
- **P3** `test-gap` (S, high) — e2e/, scripts/ and playwright.config.ts are typechecked but never linted — `package.json:16`
- **P3** `docs` (S, high) — Regional defaults survive in shared code and tests while the direction doc declares them deferred; the deferral should be a tracked item, not an aside — `packages/db/src/schema/profiles.ts:16`
- **P3** `bug` (S, medium) — proxy.ts PROTECTED set omits /settings, so unauthenticated visits fall to the layout redirect('/') and lose the ?next= return path — `src/proxy.ts:5`
- **P3** `docs` (S, high) — README and AGENTS.md omit the CI/e2e contract (three browsers, disposable DB, local-dev login prerequisite) and the docs tree has no index — `README.md:1`

## cross-cutting-dead-code-boundaries

- **P1** `release` (M, high) [confirmed] — The documented sync contract (kayamo-mobile is source of truth; ../sync-packages.sh rsync --delete into web) is already a fork in practice: 158 diverging files and 47 web-only files across packages/ (features desk/food/botanical/mus respond-handler, offline closed-DB recovery, db Lis schema, gym/session-clock) plus migrations 0021/0022 would be deleted or reverted by the next sync, breaking the shell, /api/mus/respond, /calories, /foods, /verify and the web look; ownership must be decided before the refactor — `AGENTS.md:18` (dedups tests-ci-tooling-release-5, api-routes-2, features-food-4, features-shell-desks-1, features-botanical-journey-todo-gym-2, offline-sync-2, db-and-migrations-4, tests-ci-tooling-release-2)
  - Fix: Decide the fork formally before the refactor: (1) remove kayamo-web from TARGETS in ../sync-packages.sh (or delete the script's web branch) and rewrite the AGENTS.md boundary to 'packages/ is owned by this repo'; (2) cherry-pick the mobile-only work web wants (ui/spring.ts + material/spring tokens, mus-rail face wiring) by hand, once; (3) later, if mobile resumes, extract the genuinely platform-neutral packages (core, food, db schema+queries, ai contracts/persona/safety) into a shared workspace package or git subtree, and leave features/ui as per-platform code, which they already are (features
  - Verifier: Confirmed at AGENTS.md:23-25 and ../sync-packages.sh:24-29 (rsync --delete; guard only blocks uncommitted target changes, not committed web-only work). Reproduced the divergence: 158 differing package entries (108 modified, 47 web-only, 3 mobile-only), migrations 0021/0022 web-only, mobile's last package commit 2026-09-05 vs 10 web commits since, and src/app/api/mus/respond/route.ts imports @kayam
- **P1→P2** `perf` (M, medium) [confirmed] — Root client files import the phone-app `@kayamo/features` barrel, pulling all phone screens and the 4,818-line kayamo-app.module.css into the web client graph — `src/shell/desktop-shell.tsx:12`
  - Fix: Stop importing the main barrel from src/. Move the seven web-used names (SyncStatusBar, installDemoCatalog, configureApiClient, LOCAL_DEV_EMAIL, LoginForm, SetAuthSession, CompleteAuthSession) onto `@kayamo/features/desktop` (or a new `./web` export) and add `"sideEffects": ["*.css"]` to packages/features, packages/ui, packages/db, packages/ai, packages/food, packages/offline, packages/core package.json so webpack can drop unused re-exports. Then either delete the phone screens from this repo (post-fork) or leave them only on the `.` export that nothing on web imports. Add an ESLint no-restric
  - Verifier: Confirmed the facts: six 'use client' root files (src/shell/desktop-shell.tsx:12, demo-seed.ts, offline-root.tsx, login-form.tsx, auth/complete and auth/set-session pages) import the bare '@kayamo/features' barrel, whose index.ts re-exports HomeScreen/TodayScreen/MusHabitat/GoalFlow/DiagnosticsPanel/DailyLoop etc. (~5,200 lines of phone-screen TSX); no packages/*/package.json declares sideEffects,
- **P1** `test-gap` (S, high) [reviewer-judgment] — Banned-copy hard rule is enforced only by a manual script; `pnpm check:copy` is not in CI — `.github/workflows/ci.yml:12`
  - Fix: Add `- run: pnpm check:copy` to the first CI job after lint (S). Longer term, turn the sweep into a vitest test under packages/features/src (or a root vitest test) so `pnpm test` alone guards the rule.
- **P2** `perf` (S, medium) — `@kayamo/db` root barrel exports a drizzle pgTable object, so drizzle-orm/pg-core rides into every client bundle that imports the barrel — `packages/db/src/index.ts:202`
- **P2** `dead-code` (S, high) — Nine orphan source files (about 770 lines) plus one 242-line CSS module that nothing imports — `packages/features/src/desk/gym-picker.tsx:1`
- **P2** `design` (S, high) — Lis face assets and face-rule module shipped on this branch but nothing renders them — `packages/features/src/mus/mus-faces.ts:57`
- **P2** `design` (M, high) — Three shell styling generations coexist; two are mostly or entirely dead CSS — `src/shell/shell.module.css:1`
- **P2** `design` (M, medium) — Design tokens have forked: mobile's tokens.css carries material/spring/glass tokens and spring.ts; web reimplements glass in src/app/glass.css and stripped spring motion — `packages/ui/src/tokens.css:1`
- **P2** `design` (S, high) — Language toggle was orphaned, so the Taglish choice the global-direction doc says to preserve can no longer be made in the UI — `src/shell/locale-toggle.tsx:20`
- **P2** `boundary` (S, high) — Service-role env accessor is exported from the client-safe `@kayamo/db` root barrel — `packages/db/src/index.ts:5`
- **P3** `dead-code` (S, high) — Five package export subpaths have no consumer anywhere in the web repo — `packages/features/package.json:10`
- **P3** `refactor` (M, medium) — Barrel public API is 4-10x larger than what any consumer uses; noise for the coming refactor — `packages/offline/src/index.ts:1`
- **P3** `boundary` (S, high) — Root config and scripts import package internals by relative path instead of declared exports — `next.config.ts:1`
- **P3** `bug` (S, high) — Legacy /app/* redirect map sends /app/goals, /app/life and /app/grove to the wrong routes now that those pages exist — `src/app/app/[[...slug]]/page.tsx:3`
- **P3** `boundary` (M, medium) — Root src/shell holds behaviour beyond composition: log sheet writes tasks, allowance logic, companion settings — `src/shell/log-sheet.tsx:134`
- **P3** `docs` (S, high) — .cursor/rules still say Mus, Philippines-first and Taglish while code is Lis and the Sep 14 direction is global — `.cursor/rules/010-mus-sot.mdc:8`
- **P3** `refactor` (S, medium) — 875-line seed script lives in packages/db/src and rides the package's lint/typecheck/test scope — `packages/db/src/seed.ts:1`
- **P3** `release` (S, high) — Untracked knowledge-base and audit drops at repo root need a commit-or-ignore decision before the ship PR — `.gitignore:1`

## Completeness critic additions

- **P1** `release` (S, high) — No React error boundary or not-found page anywhere in the app: a thrown render error in any shell route shows the Next default error screen with no recovery path — `src/app/layout.tsx:1`
  - Fix: Add src/app/global-error.tsx and src/app/(shell)/error.tsx (reset button, 'your data is on this device' copy, no health data in the message), src/app/not-found.tsx on the glass shell, and wire onRequestError in src/instrumentation.ts once a monitoring sink exists.
- **P1** `release` (M, high) — No error monitoring or telemetry sink of any kind; production failures in AI routes, sync and the redesign are invisible — `package.json:1`
  - Fix: Before the ship PR, add one privacy-scrubbed error sink (Sentry or Vercel's built-in error reporting) with beforeSend stripping request bodies and any food/weight fields, wire src/instrumentation.ts onRequestError for route handlers, and route the existing agent_runs fallback telemetry through it. Document in AGENTS.md that health data never enters the sink.
- **P1** `docs` (M, high) — Compliance analysis covers only the Philippine Data Privacy Act while the product direction is global; no GDPR/UK GDPR/CCPA assessment exists for a health-data app — `docs/compliance.md:1`
  - Fix: Add a jurisdiction section to docs/compliance.md (EU/UK GDPR Art. 9 special-category health data: lawful basis, DPIA trigger, DSAR handling, 72-hour breach notice to the relevant authority; CCPA/CPRA sensitive PI) and make it a named release gate alongside the legal drafts; decide the first launch geographies explicitly so the PH-only crisis footer, RA 10173 registration thresholds and retention schedule can be scoped.
- **P1** `bug` (S, high) — Correction to features-food-1: the food_id drop is in the shared offline history query, not the diary, so the fix changes seven consumers — `packages/offline/src/writes.ts:259`
  - Fix: Drop the `&& row.food_id` predicate in listLocalFoodHistory (or add a `requireCatalogFood` option used only by the verify queue) and add an offline unit test with a catalog-less entry; then re-anchor features-food-1 at this line.
- **P2** `test-gap` (S, high) — Production bundle has never been measured or budgeted; bundle-size findings rest on dev-server chunk inspection — `.github/workflows/ci.yml:19`
  - Fix: Add a CI step after `pnpm build` that prints the route size table and fails on a first-load JS budget (e.g. 250 KB gz per shell route) and a CSS budget; re-verify the kayamo-app.module.css and gym KB claims against the production output before deciding refactor priority.
- **P2** `release` (S, high) — Proxy matcher and PROTECTED set omit /settings, so the settings page is served without middleware auth gating or cookie refresh (confirms root-app-9 / tests-ci-29 which were listed as unverified) — `src/proxy.ts:5`
  - Fix: Derive both the PROTECTED set and the matcher from one exported SHELL_ROUTES constant shared with desktop-shell.tsx navigation, add '/settings', and add the missing unit test for isProtected.

## Open questions raised by reviewers

- (root-app) Is `/auth/set-session` needed by any hosted-preview e2e for the web app? The web e2e suite never uses it (only ../kayamo-mobile/e2e/helpers/auth.ts does), so it looks safe to delete from web.
- (root-app) Which bundler is the production target — Turbopack (Next default, used by `pnpm dev`) or webpack (`next build --webpack`)? Was the 'globals pipeline strips backdrop-filter' observation reproduced under the same bundler that ships?
- (root-app) Is Taglish being retired from UI chrome entirely (delete LOCALE_BOOT, login heading branch, locale-toggle) or moved into Settings as a language row? The code currently does half of each.
- (root-app) Does botanical.css need to survive on web for parity with kayamo-mobile, or can web drop it now that glass.css overwrites every --color-* token it defines?
- (root-app) The demo catalog is `source: z.literal('ph_core')` only (packages/features/src/food/demo-catalog.ts:9); is a Philippines-only demo catalog acceptable behind a global 'Explore the demo' CTA?
- (root-app) The phone tab bar's centre `+` Log button contradicts 010-mus-sot.mdc 'No sixth primary add tab'; is this an approved deviation to record, or should create move under Home?
- (root-app) src/app/dev/proposal-card is the only consumer of ProposalCard and its comment says no real screen renders a Lis-proposed mutation yet; is 'Lis proposes, you confirm' rendered anywhere on web today, and should the gallery stay until it is?
- (root-app) Account deletion and data export are marked 'Not available on the web yet' — is the launch plan an email-based deletion path (docs/legal/support-and-issue-reporting.md) or an in-app flow, and which jurisdictions' requirements are being targeted first?
- (api-routes) Is the kayamo.ph domain still owned and renewed by the project? If not, its presence in KAYAMO_PRODUCTION_API_ORIGINS (api-origin.ts:29) is a token-exfiltration risk for any Capacitor build; if yes, this is just stale branding.
- (api-routes) Which Vercel plan and compute mode (Fluid or classic) is kayamo.fit on? This decides whether the 15s/30s in-app AI timeouts can ever fire before the platform kills the function (maxDuration finding).
- (api-routes) Is /api/foods/parse meant to reach the LLM before ship, or is the heuristic the product decision? The route has been silently heuristic-only since at least Sep 8.
- (api-routes) Who owns web-only server handlers (respond-handler.ts, and the drifted server-context/server-plan/openai-provider/coco-router)? Upstream to kayamo-mobile before the next sync, or change AGENTS.md and sync-packages.sh to exclude them?
- (api-routes) Is PHOTO_ANALYSES_PER_DAY (present in .env.local, read nowhere) a planned separate photo cap, or leftover? It affects whether observe-image needs its own allowance kind.
- (api-routes) Should the login flow honour the `next` param that src/proxy.ts sets (currently ignored; afterAuthPath is hard-coded to /today)? If yes, the paths.ts guard must be hardened first.
- (features-food) Is /verify meant to ship to end users as a source-agnostic 'verify your catalog' surface, or is it a curator tool that should be gated? It is in LIFE_ROUTES nav today and filters to source === 'ph_core' only.
- (features-food) Is the `verify_ph_core_food` SECURITY DEFINER RPC still planned? verify-rpc.ts and the overlay assume it; supabase/ has no migration for it. Without it the overlay leaks edited numbers into food_entries under ph_core provenance.
- (features-food) Which repo should own the desktop-only surface (today-table/diary, foods-table/model, verify-*, command-log*, desk.module.css, desk/ planner+gym)? Mobile lacks nine of these files, so sync-packages.sh cannot run against web without breaking the routes. A web-owned package excluded from SHARED is the smallest fix.
- (features-food) Does anything outside packages/features/src/food call /api/foods/parse? Its only caller in this area is the never-rendered ConversationalLog; if none, the route and its LLM parse path can go with it.
- (features-food) Should TodayLog, FoodSearch, QuantitySheet, AddProductForm and FoodHistory stay exported from @kayamo/features on web at all, or move under a mobile-only subpath so the web dependency graph, Tailwind scan and tests reflect what ships?
- (features-food) How does the botanical redesign intend to treat the Tailwind-utility components in the same directory (today-log, food-search, quantity-form, add-product-form) if they ever render on web, given the desk uses CSS modules with a different token vocabulary?
- (features-shell-desks) Is packages/features/src/desk (plus screens/mus-thread.tsx, i18n, desktop.ts, ports.ts) meant to be web-owned, or should the web desks be upstreamed into ../kayamo-mobile? The rsync --delete behavior makes this the first decision the refactor needs.
- (features-shell-desks) Are /gym and /todos in scope for the botanical redesign before ship, or should Movement/Todos be hidden from RAIL and Life until they are restyled?
- (features-shell-desks) Is the shell-level compact Lis rail coming back (DeskMusPane, mus-selection and SCREEN_LABEL exist only for it), or should that context-publishing layer be deleted along with MusRailMount?
- (features-shell-desks) Should the Taglish toggle be resurfaced in Settings as a language preference under the global direction, or removed entirely?
- (features-shell-desks) Should Pause session persist across navigation and reload (requires an offline schema field), or be removed before ship?
- (features-shell-desks) Do you want the mobile shell files (app-screens, daily-loop, diagnostics, use-local-app-snapshot) kept in the web repo purely as synced copies, or excluded from the web build via sideEffects and a narrower entry point?
- (features-botanical-journey-todo-gym) Are botanical/, journey/ and gym/session-clock.ts intended to be web-owned (mobile keeps screens/app-screens.tsx) or should the web versions be pushed back to kayamo-mobile before the next sync? The answer decides whether they move out of the rsync'd packages/ tree.
- (features-botanical-journey-todo-gym) Is the Grove 'Streak' concept wanted at all, given the SoT frames Grove as a record and the same screen says 'not a streak to maintain'?
- (features-botanical-journey-todo-gym) Is a tree/stage illustration in scope for the web ship, or should the design docs state that Grove is text-only on the web?
- (features-botanical-journey-todo-gym) Should the Goals editor be rewritten on botanical primitives before ship (L effort) or is a visually-patched GoalFlow acceptable for the first release?
- (features-botanical-journey-todo-gym) Will the route slug follow the rename (/lis with /mus redirect), and is 'Lis' final enough to update the .cursor rules that still say Mus?
- (features-botanical-journey-todo-gym) Is Life Story part of the web product? If yes, goals.tsx needs onAddToStory wired; if no, the Grove panel should be hidden.
- (features-botanical-journey-todo-gym) Should the todo knowledge base HARD rules be injected into the planner prompt (their stated purpose) or should the module be deleted?
- (ai-and-mus) docs/audits/2026-09-15-lis-qa.md Severity 2 says `mus_context_permissions` did not exist in the deployed database; the migration exists in supabase/migrations/0018 but I could not verify whether it has been applied to production (kayamo.fit). If not, every Lis turn runs with permissionLookupFailed and an empty context. Needs a DB check before ship.
- (ai-and-mus) Is the mascot retired for good (e2e/mus.spec.ts, botanical/icons.tsx) or is the botanical redesign reinstating a face (staged public/botanical/mus-*.webp and prompts suggest yes)? The answer decides whether mus-faces.ts is deleted or wired.
- (ai-and-mus) Is `perm-levels.ts` (five write levels per module) a planned feature or a leftover from the Aug desk build? Nothing writes it today.
- (ai-and-mus) Which regions' crisis lines is the owner willing to vet for the global launch? The neutral footer is a deliberate choice, but the QA audit rated the PH-only footer Severity 1.
- (ai-and-mus) Live evals: has anyone re-recorded baselines after LIS_PERSONA_VERSION 3? recordedAt is 2026-09-15 and the persona rewrite commit f782897 is later in history than the recording commit a9e636b; without personaVersion in the JSON this cannot be confirmed from the repo.
- (ai-and-mus) The mobile source of truth (../kayamo-mobile/packages/ai) still ships 'You are Mus' and has no persona.ts, food-parse.ts, context-limits.ts or evals/. Sync direction for packages/ai needs an explicit decision (web -> mobile for these 25 files) before ../sync-packages.sh can run again.
- (offline-sync) Is the eight-second Undo after deleting a food entry a product requirement? If yes, tombstone pushes must be delayed by the undo window (or a server restore path added); if no, the Undo affordance in today-table.tsx and conversational-log.tsx should be removed rather than left silently failing when online.
- (offline-sync) Should the web-only closed-DB recovery (recoverClosedOfflineDb triple retry, global unhandledrejection swallow, liveQuery wrapper) be upstreamed to kayamo-mobile or recorded as a permanent web-only exception? The sync script will revert it otherwise.
- (offline-sync) Does kayamo-mobile app code (outside packages/) import any mergeRemote* function, logWorkout, useLivePlanningSnapshot or resetOfflineDb? Deleting them from the shared package needs that check.
- (offline-sync) Is it intentional that demo (guest) data is never migrated into the account on sign-in and never deleted afterward? The shell copy says 'demo entries won't transfer', but the guest database is left on the device indefinitely.
- (offline-sync) Does the draft privacy policy promise device-local data deletion on sign-out or on request? If so the 'remove my data from this device' action is a release gate, not a P2.
- (offline-sync) Will the botanical Home use the planning snapshot (getPlanningSnapshot/useLivePlanningSnapshot) or the 26-query useLocalAppSnapshot? If neither, both can be deleted before the refactor; if one will, it should move to features rather than stay in offline.
- (db-and-migrations) Has migration 0018 been applied to the hosted project since the 2026-09-15 QA found it missing, and have 0021 and 0022 been applied? Nothing in the repo records either; the P0 finding stands until `supabase migration list` against production says otherwise.
- (db-and-migrations) Is packages/db meant to become web-owned (all Lis schema work landed here first), or will 0021/0022 and the eight db files be ported to kayamo-mobile before the next ../sync-packages.sh run?
- (db-and-migrations) Is drizzle-kit still intended as a migration runner at all, or is the Supabase CLI the sole runner (as CI implies)? The answer decides whether supabase/migrations/meta/ is deleted or regenerated with snapshots.
- (db-and-migrations) Should new global users get locale 'en' and their browser timezone at first sign-in (migration 0023 + a shell write), or is Asia/Manila/taglish still the intended default for everyone?
- (db-and-migrations) Does the offline package intentionally treat an upsert result of `{ applied: false, row: null }` as success, or is the silent drop on same-day weight logs (and other live-unique-key conflicts) a latent data-loss bug the offline reviewer should confirm?
- (db-and-migrations) Which tables are the 30 skipped unit tests in the baseline — presumably the three live-DB suites in packages/db; confirm CI's database-integration job is green on this branch so the RLS matrix is actually exercised.
- (food-core-domain) Do any usda_fdc rows with dataType Branded exist in the production foods table from before the per-100g fix (i.e. did the USDA key ever work in prod)? This decides whether the cache audit is a ship blocker or a no-op.
- (food-core-domain) Is the web meant to compute guidance itself (port mobile's src/lib/guidance-server.ts and /api/guidance) or to depend on the mobile app having written targets? Web-only accounts currently can never receive a target.
- (food-core-domain) Which rule wins on LLM estimates: 000-project.mdc ('never from the model') or 400-nutrition-domain.mdc rung 6 ('LLM estimate — last resort')? The resolve.ts hook and the source='llm' photo-range UI depend on the answer.
- (food-core-domain) Is contact@kayamo.ph a monitored mailbox? Open Food Facts requires a reachable contact in the User-Agent and blocks abusive clients.
- (food-core-domain) Should PH-first serving order (sortServingsPhFirst) and en-PH number formatting stay as an explicit 'Filipino cuisine' behaviour, or follow the viewer's locale under the global direction?
- (food-core-domain) Is data/ph-core/prices.yaml consumed anywhere (planner agent) on web or mobile, or is it a leftover from the Philippines-first plan?
- (design-system-and-css) Naming decision: the working tree renders Liquid Glass everywhere while branch, docs and module names say 'botanical'. Should the finished system be called botanical (palette from mobile's tokens.css) or glass (what the code does)? The consolidation plan works either way but the token values differ (#246348 forest vs #30d158 iOS green).
- (design-system-and-css) Sync intent: are packages/features and packages/ui still meant to be shared with kayamo-mobile? desk.module.css differs by 4,520 lines and mobile has none of the glass tokens; if sharing is still the goal, which side is the source of truth for styling?
- (design-system-and-css) Is the 'Tailwind/Lightning pipeline strips backdrop-filter' claim real? A single `pnpm build` + grep of .next/static/css answers it and decides whether glass-materials.module.css can merge into glass.css.
- (design-system-and-css) Is Tailwind staying? If the six primitives move to CSS modules, nothing on a web route needs it beyond preflight.
- (design-system-and-css) Is the 390px GoalFlow (kayamo-app.module.css) meant to survive on web, or should the goal editor be rebuilt in the glass dialog like the task editor already is?
- (design-system-and-css) Should proposals keep a distinct colour (mustard --color-proposal) now that glass.css aliases it to the accent? The product rule 'Mus proposes, user confirms' had a visual carrier that is gone.
- (tests-ci-tooling-release) Which branch does Vercel promote to production — origin/main (where the deployed commit e7326d8 lives) or feat/web (the declared PR target, 12 commits behind main)? The answer decides whether the PR must be retargeted or feat/web fast-forwarded first.
- (tests-ci-tooling-release) Have migrations 0018 (reported missing by the 2026-09-15 Lis QA), 0021 and 0022 been applied to the hosted Supabase project, and through which repo's process? No doc records it.
- (tests-ci-tooling-release) Is DATABASE_URL in kayamo-web/.env.local the production database? scripts/web-ai-allowance.mjs asserts that hostname on the same variable, which would make `pnpm ph-core:build` and `seed.ts` production writes by default.
- (tests-ci-tooling-release) Is the Lis mascot retired for the web (e2e/mus.spec.ts asserts every mascot image is gone) or kept as optional web overrides (release-candidate.md and the staged mus-*.webp say so)? The answer determines whether ~3.6 MB of public assets and the prompts/evidence are dead.
- (tests-ci-tooling-release) Should the local-dev skip-login (which needs a service-role key on the developer machine) exist in the web repo at all, or should local development target the local Supabase started with `supabase start`?
- (tests-ci-tooling-release) Are the 2026-09-08/09/13 audit folders (1.8 MB PNG, untracked) meant to stay unpublished as docs/releases/2026-09-10-web.md states, or to be committed like 2026-09-15-lis-qa.md? One rule is needed before the archive move.
- (tests-ci-tooling-release) Which of the 158 diverged shared-package files are approved web-only exceptions? Only prose in implementation-status.md records the decision; sync-packages.sh needs a machine-readable list or a src/ relocation.
- (tests-ci-tooling-release) Does the owner want the e2e suite to run against the production build in CI (requires replacing the NODE_ENV gate on local-dev login with an explicit env flag or storageState), accepting the small change to packages/features/src/auth/local-dev.ts in the canonical repo?
- (cross-cutting-dead-code-boundaries) Fork or sync: is kayamo-mobile still an active target? Its packages/ have not been committed since Sep 5 and its working tree carries ~45 uncommitted package edits (spring motion, material tokens, mus-rail faces) that web does not have. The answer decides whether gym-picker.tsx, mus-faces.ts, the phone screens and the `@kayamo/features` main barrel are dead code on web or shared code to keep.
- (cross-cutting-dead-code-boundaries) Should Taglish remain a user-selectable chrome language on web (then a control must be mounted), or is English-only acceptable under the Sep 14 global direction (then desk-locale.ts and the DESK_LOCALES exports can go)?
- (cross-cutting-dead-code-boundaries) Which liquid-glass implementation is canonical for the botanical finish: mobile's token-driven --material-*/--spring-* set in @kayamo/ui/tokens.css plus spring.ts, or web's src/app/glass.css plus the simplified WAAPI sheet motion?
- (cross-cutting-dead-code-boundaries) Is the /app/* legacy catch-all still needed for links on the production deploy at kayamo.fit (older commit)? If yes its map needs the /goals, /life, /grove, /settings routes; if no it can be deleted.
- (cross-cutting-dead-code-boundaries) Barrel dead-export counts are regex-derived upper bounds (type-only exports included, one dynamic import in @kayamo/offline); confirm with ts-prune/knip before deleting implementations.
